---
quick_id: "260507-004"
slug: "document-run-on-each-device"
description: "Document how to run the app on each device"
date: "2026-05-07"
---

# Quick Task Plan: Per-Device Run Instructions

## Context

Quick tasks 260507-002/003 made the app runnable on Mac and easily installable on Steam Deck, but the README only carries the short version (a Steam Deck one-liner + a Mac dev block). Need a single canonical doc that covers each device end-to-end.

## Task 1: Write `docs/RUNNING.md`
- **files**: `docs/RUNNING.md` (new)
- **action**: Single doc, three device sections + troubleshooting matrix:
  - **Steam Deck (end user)**: Desktop Mode install (curl one-liner + manual fallback), Add Non-Steam Game flow, Gaming Mode launch, source-build escape hatch via `build-steamdeck.sh`, update flow.
  - **macOS (dev)**: prereqs (Big Sur+, Xcode CLT, Node 18+, pnpm 10, Rust stable), `pnpm install && pnpm dev`, `NSBluetoothAlwaysUsageDescription` prompt walkthrough, recovery if denied, local `tauri:build` for `.app`/`.dmg`, Gatekeeper unsigned-app bypass.
  - **Linux (dev)**: pacman + apt prereqs, `pnpm dev`, `input` group for evdev access, local AppImage build.
  - **Troubleshooting cheat sheet**: BT24 not advertising, Mac BT denied, Gaming-Mode crash, deadzone direction, BLE-without-write, linker errors, Gamescope evdev visibility.
  - **File map**: where BLE / gamepad / config / installer live.
- **verify**: doc parses as markdown; no broken cross-references; covers all three devices.
- **done**: false

## Task 2: Fix arch error in `docs/STEAM_DECK.md`
- **files**: `docs/STEAM_DECK.md`
- **action**: Steam Deck APU is AMD x86_64 (Van Gogh / Sephiroth on OLED), not ARM. Replace the `aarch64-unknown-linux-gnu` build commands and `ubuntu-22.04-arm` CI snippet with x86_64 equivalents. Note that the CI's existing arm64 job exists for SBC-class ARM Linux boxes, not the Deck.
- **verify**: doc no longer claims Deck CPU is aarch64.
- **done**: false

## Task 3: Cross-link `docs/RUNNING.md` from `README.md`
- **files**: `README.md`
- **action**: Add a one-line callout under the project description pointing to `docs/RUNNING.md` for the deep guide. Keep the existing short install/dev sections — they're useful at-a-glance.
- **verify**: README contains a markdown link to `docs/RUNNING.md`.
- **done**: false

## Out of scope
- Windows support (Tauri builds, but we haven't tested btleplug on Windows for this BT24 device — separate decision).
- Notarizing the macOS bundle.
- Publishing the AppImage to flathub or AUR.
