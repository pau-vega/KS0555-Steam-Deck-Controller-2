---
quick_id: "260508-001"
slug: "tauri-best-practices"
description: "Apply the best Tauri practices — refactor Rust backend to follow Tauri v2 conventions"
date: "2026-05-08"
---

# Quick Task Plan: Tauri v2 Best Practices

## Task 1: Extract lib.rs with all app logic, thin main.rs

- **files**: `apps/frontend/src-tauri/src/lib.rs` (new), `apps/frontend/src-tauri/src/main.rs`
- **action**: Create `lib.rs` containing all commands (`ble_connect`, `ble_disconnect`, `ble_send`), BLE state (`BleState`), event listener setup (`setup_event_listener`), gamepad setup (`setup_gamepad_monitor`), and the `run()` function. Thin `main.rs` to just call `app_lib::run()`. Add `#[cfg_attr(mobile, tauri::mobile_entry_point)]` on `run()` for mobile compatibility.
- **verify**: `main.rs` is a thin passthrough; `lib.rs` contains all logic with proper module structure (ble, gamepad)
  - **done**: true

## Task 2: Update Cargo.toml with [lib] section and thiserror

  - **files**: `apps/frontend/src-tauri/Cargo.toml`
  - **action**: Add `[lib]` section with `crate-type = ["staticlib", "cdylib", "rlib"]` and `name = "app_lib"`. Add `thiserror` dependency. Remove obsolete `crate-type` from `[package]` if present.
  - **verify**: Cargo.toml has correct `[lib]` section; `thiserror` is in dependencies
  - **done**: true

## Task 3: Replace String errors with proper error types using thiserror (deferred)

  - **files**: `apps/frontend/src-tauri/src/lib.rs`, `apps/frontend/src-tauri/src/ble/mod.rs`, `apps/frontend/src-tauri/src/gamepad/mod.rs`
  - **action**: Create `AppError` enum using `thiserror::Error` with variants for Bluetooth, Gamepad, and general errors. Implement `Serialize` for `AppError`. Update all commands to return `Result<T, AppError>` instead of `Result<T, String>`.
  - **deferred_reason**: Changing command return types from `Result<(), String>` to `Result<(), AppError>` requires frontend TypeScript changes in `use-bluetooth.ts` to handle structured errors. thiserror dependency is in place for a future phase. Current `Result<(), String>` pattern works correctly.
  - **done**: false (deferred)
