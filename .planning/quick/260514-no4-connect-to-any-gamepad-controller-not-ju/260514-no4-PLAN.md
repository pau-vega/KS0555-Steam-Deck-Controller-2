---
phase: quick-no4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/hooks/use-gamepad.ts
  - apps/frontend/src/hooks/use-gamepad.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Hook no longer references Steam Deck vendor/product IDs"
    - "Hook no longer calls navigator.getGamepads() (browser API dead in Tauri WebView)"
    - "Hook exposes gamepadName from gamepad-connected event payload"
    - "Any gamepad (8BitDo, Xbox, PS, Steam Deck, generic) connects and shows its name"
  artifacts:
    - path: "apps/frontend/src/hooks/use-gamepad.ts"
      provides: "Gamepad hook cleansed of Steam-Deck-only dead code, exposes controller name"
      min_lines: 60
    - path: "apps/frontend/src/hooks/use-gamepad.test.ts"
      provides: "Tests verify gamepadName capture and reset"
  key_links:
    - from: "use-gamepad.ts gamepad-connected listener"
      to: "Rust gamepad/mod.rs emit gamepad-connected"
      via: "Tauri IPC event"
      pattern: "listen.*gamepad-connected"
---

<objective>
Remove dead Steam-Deck-only detection code from the gamepad hook. Capture and expose the
gamepad name from the `gamepad-connected` event so any controller (8BitDo Ultimate 2,
Xbox, PS, generic) shows its identity. The Rust side already supports all gamepads via
gilrs with no name filter — this task fixes the frontend to stop implying Steam Deck is special.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/frontend/src-tauri/src/gamepad/mod.rs (Rust emits gamepad-connected with { name: string })
@apps/frontend/src/app.tsx (locked — cannot modify)
@apps/frontend/src/types.ts (Direction type only, no changes needed)

<interfaces>
From use-gamepad.ts current return:
  { direction: Direction, gamepadConnected: boolean, isDeck: boolean }

From Rust gamepad-connected event payload (via Tauri IPC):
  { name: string }

Hook contract (AGENTS.md): adding fields is fine; renaming/removing is not.
app.tsx destructures only { direction, gamepadConnected } — does NOT consume isDeck.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clean use-gamepad.ts — remove dead Steam Deck detection, expose gamepadName</name>
  <files>apps/frontend/src/hooks/use-gamepad.ts</files>
  <behavior>
    - Test 1: gamepad-connected event with { name: "8BitDo Ultimate 2" } sets gamepadName to "8BitDo Ultimate 2"
    - Test 2: gamepad-disconnected event clears gamepadName to null
    - Test 3: initial state has gamepadName: null, gamepadConnected: false, direction: "S"
    - Test 4: isDeck remains in return (always false) for backward compat — no consumer uses it
  </behavior>
  <action>
    Remove dead browser-API code that does not work in Tauri WebView:
    - Delete constants STEAM_DECK_VENDOR_ID, STEAM_DECK_PRODUCT_ID
    - Delete function isSteamDeck(gamepad: Gamepad)
    - Delete function detectSteamDeck() and all calls to it (on setup + on connected event)
    - Delete navigator.getGamepads?.() usage — this is a browser API, Tauri WebView does not expose the Gamepad API

    Add gamepadName state and capture it from event payload:
    - Import: keep existing imports, no new dependencies
    - State: add `const [gamepadName, setGamepadName] = useState<string | null>(null)`
    - On "gamepad-connected" listener: call `setGamepadName(event.payload.name)` alongside existing `setGamepadConnected(true)`
    - On "gamepad-disconnected" listener: call `setGamepadName(null)` alongside existing resets
    - Remove the `detectSteamDeck()` call that currently runs after setGamepadConnected(true)

    Return value:
    - Add `gamepadName` to return object: `return { direction, gamepadConnected, gamepadName, isDeck: false }`
    - Keep `isDeck: false` literal (always false) for backward compat with hook contract.
      No consumer destructures `isDeck` (app.tsx does not, App.test.tsx mock does not),
      but the hook contract rule says renaming/removing is not allowed.
  </action>
  <verify>
    <automated>pnpm --filter @ks0555/frontend test -- use-gamepad.test.ts</automated>
  </verify>
  <done>
    Hook no longer imports or calls navigator.getGamepads(). No references to Steam Deck vendor/product
    IDs remain. gamepadName is null initially, set to payload.name on connected, cleared on disconnected.
    All 5 existing tests pass + 3 new gamepadName tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update use-gamepad.test.ts — add gamepadName coverage</name>
  <files>apps/frontend/src/hooks/use-gamepad.test.ts</files>
  <behavior>
    - Test A: gamepad-connected with name payload → result.current.gamepadName equals that name
    - Test B: gamepad-connected then gamepad-disconnected → gamepadName resets to null
    - Test C: initial render has gamepadName: null
  </behavior>
  <action>
    Add three new test cases to the existing describe("useGamepad") block:

    1. "exposes gamepadName from connected event" — render hook, fire gamepad-connected with
       { name: "8BitDo Ultimate 2" }, assert result.current.gamepadName === "8BitDo Ultimate 2".

    2. "clears gamepadName on disconnect" — render hook, fire gamepad-connected with
       { name: "Xbox Controller" }, assert gamepadName is set, then fire gamepad-disconnected,
       assert result.current.gamepadName === null.

    3. "gamepadName starts null" — render hook, assert result.current.gamepadName === null
       before any events fire. (Can be combined into existing "starts with stop direction and
       disconnected" test by adding one assertion.)

    Existing tests must continue to pass with zero changes — the hook's other behavior
    (direction updates, connected/disconnected toggles, cleanup) is unchanged.
  </action>
  <verify>
    <automated>pnpm --filter @ks0555/frontend test -- use-gamepad.test.ts</automated>
  </verify>
  <done>
    All 8 tests pass (5 existing + 3 new). gamepadName null at start, populated from
    gamepad-connected payload, cleared on disconnect.
  </done>
</task>

</tasks>

<verification>
Run full test suite: `pnpm test` (Turbo, all workspaces). Verify no regressions.
Run typecheck: `pnpm typecheck`. Must pass with zero errors.
</verification>

<success_criteria>
- `use-gamepad.ts` contains zero references to `navigator.getGamepads`, `STEAM_DECK_VENDOR_ID`,
  `STEAM_DECK_PRODUCT_ID`, `isSteamDeck`, or `detectSteamDeck`
- `use-gamepad.ts` exports `gamepadName: string | null` from the hook return
- `gamepadName` is populated from the Rust-emitted `gamepad-connected` event payload
- All existing tests pass, new tests cover gamepadName behavior
- `pnpm typecheck` passes
- The Rust side (`gamepad/mod.rs`) already supports any gamepad — no changes needed there
</success_criteria>

<output>
After completion, create .planning/quick/260514-no4-connect-to-any-gamepad-controller-not-ju/260514-no4-SUMMARY.md
</output>
