---
quick_id: "260507-003"
slug: "remove-unused-and-obsolete-code"
description: "Remove unused and obsolete code"
date: "2026-05-07"
status: complete
---

# Quick Task Summary: Remove Unused and Obsolete Code

## What changed

### Deleted
- `apps/backend/` — full pre-Tauri Node Fastify+serialport WebSocket server (8 source files, tests, lockfile entries). Orphan since BLE moved into the Rust side; frontend never imported it.
- `smoke-test-dev.sh` — exercised the broken `pnpm dev` frontend+backend split; both endpoints obsolete.

### Modified
- `package.json` — dropped `dev:frontend` and `dev:backend` scripts (wrong filter + dead target); repointed `dev` to `pnpm --filter @ks0555/frontend tauri:dev` so a single `pnpm dev` starts the Tauri shell. Description retitled "Tauri v2 desktop app (Rust BLE via btleplug, gamepad via gilrs)".
- `apps/frontend/src-tauri/src/ble/mod.rs` — removed unused imports `Adapter`, `Peripheral`, `Manager as TauriManager`; prefixed unused parameters `_state`, `_app`, `_device_id`; added `#[allow(dead_code)]` to `BT24_SERVICE_UUID` (kept as the contract for D-31 service-UUID verification).
- `README.md` — dropped the `apps/backend/` row from the project-layout table.
- `pnpm-lock.yaml` — regenerated; -79 packages removed (fastify, serialport, ws, @fastify/websocket, etc.).

## Verification

- ✅ `cargo check` — zero warnings (was 6 before).
- ✅ `pnpm build` — both workspace builds succeed (eslint-config + frontend).
- ✅ `pnpm typecheck` — clean.
- ✅ `pnpm lint` — clean (pre-existing `.eslintignore` deprecation notice unchanged).
- ✅ `pnpm test` — 43/43 frontend tests pass across 6 files.
- ✅ `cargo test` — 7 of 8 cargo test files green; `tests/ble_linux_filter_test.rs::test_service_uuid_verification_optional` is **pre-existing** red (asserts a literal string `"BT24 service UUID verified"` that never appeared in `src/ble/mod.rs` at HEAD before this cleanup). Confirmed unrelated by `git show HEAD~1:.../mod.rs | grep "BT24 service UUID verified"` (no match).

## Files touched

| Status | Path                                                           |
|--------|----------------------------------------------------------------|
| del    | `apps/backend/` (8 files + dir)                                |
| del    | `smoke-test-dev.sh`                                            |
| mod    | `package.json`                                                 |
| mod    | `apps/frontend/src-tauri/src/ble/mod.rs`                       |
| mod    | `README.md`                                                    |
| mod    | `pnpm-lock.yaml`                                               |
| new    | `.planning/quick/260507-003-…/260507-003-PLAN.md`              |
| new    | `.planning/quick/260507-003-…/260507-003-SUMMARY.md` (this)    |

## Follow-ups (still out of scope)

1. `tests/ble_linux_filter_test.rs::test_service_uuid_verification_optional` — pre-existing mismatch between assertion and source. Either drop the test or implement the missing service-UUID verify branch in `ble_connect`.
2. `docs/STEAM_DECK.md` claims Deck is aarch64; it's x86_64.
3. `CLAUDE.md` carries upstream `base-monorepo-template` boilerplate (TypeScript 6 migration, showcase app, `@monorepo-template/ui`). Misleading for future sessions; needs a project-specific rewrite.
