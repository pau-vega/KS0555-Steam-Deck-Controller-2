---
quick_id: "260507-003"
slug: "remove-unused-and-obsolete-code"
description: "Remove unused and obsolete code"
date: "2026-05-07"
---

# Quick Task Plan: Remove Unused and Obsolete Code

## Context

Quick task 260507-002 flagged `apps/backend/` as orphan and Rust dead-code warnings as follow-up. App is fully Tauri now (Rust BLE via btleplug, gamepad via gilrs); the Node Fastify+serialport+ws backend has no runtime path.

## Task 1: Delete legacy backend pkg + smoke test
- **files**: `apps/backend/` (entire dir), `smoke-test-dev.sh`
- **action**: `git rm -rf` both. Frontend never imports `@ks0555/backend`, only its own `package.json` references the name (self). The smoke-test script invokes `pnpm dev` against frontend+backend split — broken since the backend dies and `pnpm --filter frontend` uses wrong filter (real name is `@ks0555/frontend`).
- **verify**: `apps/` contains only `frontend/`; no `apps/backend` references remain except in `.planning/` (historical artifacts kept).
- **done**: false

## Task 2: Clean root package.json
- **files**: `package.json`
- **action**:
  - Drop `dev:frontend` (wrong filter) and `dev:backend` (target gone).
  - Repoint `dev` from `turbo dev` to `pnpm --filter @ks0555/frontend tauri:dev` so the Tauri shell starts directly.
  - Update `description` to drop "Node.js backend" wording.
- **verify**: `grep dev:backend\\|dev:frontend` returns no matches; `pnpm dev` starts the Tauri shell.
- **done**: false

## Task 3: Fix Rust dead-code in `src-tauri/src/ble/mod.rs`
- **files**: `apps/frontend/src-tauri/src/ble/mod.rs`
- **action**:
  - Remove unused imports: `Adapter`, `Peripheral` (from `btleplug::platform`), `Manager as TauriManager` (from `tauri`).
  - Prefix unused parameters with `_`: `_state` in `setup_event_listener`, `_app` in `ble_send`, `_device_id` in the `CentralEvent::DeviceDiscovered` match arm.
  - Mark the deferred `BT24_SERVICE_UUID` const with `#[allow(dead_code)]` and a one-liner explaining it's reserved for D-31 (optional service-UUID verification). Avoid deleting — the const is the contract for future enhancement.
- **verify**: `cargo check` produces zero warnings.
- **done**: false

## Task 4: Strip backend row from README + regen lockfile
- **files**: `README.md`, `pnpm-lock.yaml`
- **action**: Drop the `apps/backend/` row from the project-layout table. Run `pnpm install` so the lockfile drops the 79 backend-only deps (fastify, serialport, @fastify/websocket, ws, @types/ws, tsx, vitest in that workspace, etc.).
- **verify**: README does not contain `apps/backend`; lockfile diff shows ~600 line reduction.
- **done**: false

## Task 5: Run full quality gates
- **files**: none
- **action**: `pnpm build && pnpm typecheck && pnpm lint && pnpm test && cargo check`. Each must succeed.
- **verify**: All commands exit 0; vitest reports 43 frontend tests passing.
- **done**: false

## Out of scope
- Cargo integration tests under `apps/frontend/src-tauri/tests/` — one (`test_service_uuid_verification_optional`) was already red before this cleanup (it expects a literal string `"BT24 service UUID verified"` that has never existed in `mod.rs` at any commit on this branch). Pre-existing structural-test mismatch from phase 10's D-51 strategy; logged as a follow-up, not addressed here.
- `docs/STEAM_DECK.md` saying the Deck is aarch64 (it's x86_64) — separate doc fix.
- `CLAUDE.md` — still carries the upstream `base-monorepo-template` boilerplate (TypeScript 6 migration, showcase app, `@monorepo-template/ui`). Project memory rewrite is a bigger scope; flag for a future session.
