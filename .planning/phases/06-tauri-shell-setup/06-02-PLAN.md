---
phase: "6-tauri-shell-setup"
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "apps/frontend/vite.config.ts"
autonomous: true
requirements: ["TAUR-04"]
user_setup: []
must_haves:
  truths:
    - "Vite dev server starts on port 5173 with strictPort enabled"
    - "Vite ignores src-tauri/ directory in watch mode"
    - "clearScreen is disabled to preserve Rust error output"
  artifacts:
    - path: "apps/frontend/vite.config.ts"
      provides: "Vite configuration with Tauri integration"
      contains: "clearScreen: false"
    - path: "apps/frontend/vite.config.ts"
      provides: "Port 5173 configured for Tauri devUrl match"
      contains: "port: 5173"
  key_links:
    - from: "apps/frontend/vite.config.ts"
      to: "apps/frontend/src-tauri/tauri.conf.json"
      via: "devUrl http://localhost:5173 must match Vite port"
      pattern: "localhost:5173"
---

<objective>
Configure Vite for Tauri integration with proper dev server settings.

Purpose: Ensure Vite dev server works seamlessly with Tauri's development workflow, matching the devUrl in tauri.conf.json.

Output: Updated vite.config.ts with Tauri-specific settings (clearScreen, strictPort, port 5173, watch ignore).
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/.planning/PROJECT.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/.planning/ROADMAP.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/.planning/REQUIREMENTS.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/.planning/phases/06-tauri-shell-setup/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Vite for Tauri Integration</name>
  <files>apps/frontend/vite.config.ts</files>
  <read_first>
    - Read apps/frontend/vite.config.ts to see current configuration
    - Reference CONTEXT.md D-21 for exact Vite settings required
    - Reference Tauri v2 docs: clearScreen: false, strictPort: true, port: 5173, watch.ignore: ['src-tauri/**']
  </read_first>
  <action>
    Update `apps/frontend/vite.config.ts` (per D-21, TAUR-04):
    
    ```typescript
    import tailwindcss from '@tailwindcss/vite'
    import react from '@vitejs/plugin-react'
    import { defineConfig } from 'vite'

    export default defineConfig({
      plugins: [react(), tailwindcss()],
      resolve: {
        alias: {
          '@': '/src'
        }
      },
      // Tauri integration settings (per D-21)
      clearScreen: false,  // Preserve Rust errors in terminal
      server: {
        strictPort: true,    // Tauri expects fixed port 5173
        port: 5173,          // Must match devUrl in tauri.conf.json
        watch: {
          ignored: ['**/src-tauri/**']  // Don't reload on Rust changes
        }
      }
    })
    ```
    
    Verify the configuration is valid by running `cd apps/frontend && npx vite --host 2>&1 | head -20` to check for config errors.
  </action>
  <verify>
    <automated>grep -q "clearScreen: false" apps/frontend/vite.config.ts && echo "PASS: clearScreen set"</automated>
    <automated>grep -q "strictPort: true" apps/frontend/vite.config.ts && echo "PASS: strictPort set"</automated>
    <automated>grep -q "port: 5173" apps/frontend/vite.config.ts && echo "PASS: port 5173 set"</automated>
    <automated>grep -q "src-tauri" apps/frontend/vite.config.ts && echo "PASS: watch ignore set"</automated>
  </verify>
  <done>
    - vite.config.ts has clearScreen: false
    - vite.config.ts has strictPort: true
    - vite.config.ts has port: 5173
    - vite.config.ts watch.ignored includes src-tauri/**
    - Vite config is valid (no syntax errors)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vite Dev Server ↔ Tauri | Tauri spawns Vite dev server via beforeDevCommand, connects to http://localhost:5173 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-------------|-------------|-----------------|
| T-6-06 | Spoofing | Vite dev server port | accept | Development only; production uses bundled AppImage with no external dev server |
| T-6-07 | Tampering | WebSocket HMR connection | accept | Vite HMR on localhost:5173 only accessible locally; Tauri WebView loads from same origin |
| T-6-08 | Information Disclosure | Vite build output | mitigate | Production build outputs to dist/ which is bundled into Tauri AppImage; no external access |
</threat_model>

<verification>
1. `grep -q "clearScreen: false" apps/frontend/vite.config.ts && echo "PASS: Vite Tauri integration"`
2. `grep -q "port: 5173" apps/frontend/vite.config.ts && echo "PASS: Port matches tauri.conf.json"`
3. `cd apps/frontend && npx vite build --mode development 2>&1 | grep -q "error" && echo "FAIL" || echo "PASS: Vite config valid"`
</verification>

<success_criteria>
- [ ] vite.config.ts has clearScreen: false
- [ ] vite.config.ts has strictPort: true, port: 5173
- [ ] vite.config.ts watch.ignored includes ['src-tauri/**']
- [ ] Vite config loads without errors
- [ ] Port 5173 matches devUrl in tauri.conf.json (TAUR-02, TAUR-04 alignment)
</success_criteria>

<output>
After completion, create `.planning/phases/06-tauri-shell-setup/06-02-SUMMARY.md`
</output>
