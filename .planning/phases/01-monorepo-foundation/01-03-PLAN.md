---
phase: 01-monorepo-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - apps/frontend/package.json
  - apps/frontend/tsconfig.json
  - apps/frontend/vite.config.ts
  - apps/frontend/src/App.tsx
  - apps/frontend/src/App.test.tsx
  - apps/frontend/vitest.config.ts
  - apps/frontend/index.html
  - apps/frontend/src/index.css
autonomous: true
requirements: [MONO-01, MONO-03, MONO-04]
user_setup: []
must_haves:
  truths:
    - Frontend app has Vite + React working
    - TypeScript config extends shared packages/tsconfig
    - Tailwind CSS is configured (D-13)
    - ESLint extends shared packages/eslint-config (D-12)
    - Vitest + @testing-library/react is configured (D-14)
    - Path alias `@` maps to `src/` (D-15)
    - `pnpm dev` starts frontend with Vite
  artifacts:
    - path: "apps/frontend/package.json"
      provides: "Frontend package with React, Vite, Tailwind, Vitest"
      contains: '"vite":'
    - path: "apps/frontend/tsconfig.json"
      provides: "TypeScript config extending shared tsconfig"
      contains: '"extends": "@ks0555/tsconfig/tsconfig.react.json"'
    - path: "apps/frontend/vite.config.ts"
      provides: "Vite config with Tailwind plugin and path aliases"
      contains: "tailwindcss"
    - path: "apps/frontend/src/App.tsx"
      provides: "React App component"
      contains: "App"
  key_links:
    - from: "apps/frontend/tsconfig.json"
      to: "packages/tsconfig/tsconfig.react.json"
      via: "extends field"
      pattern: '"extends": ".*tsconfig.react.json"'
    - from: "apps/frontend/vite.config.ts"
      to: "packages/eslint-config/src/react.ts"
      via: "eslint config extends"
      pattern: "eslint-config.*react"
    - from: "apps/frontend/src/App.tsx"
      to: "@/components" (future)
      via: "path alias @"
      pattern: "from '@/"
---

<objective>
Set up frontend app with Vite + React + TypeScript, Tailwind CSS, ESLint, and Vitest.
</objective>

<purpose>
Creates the frontend workspace with Vite + React (D-09), TypeScript extending shared config (D-10), Tailwind CSS (D-13), and testing infrastructure (D-14).
</purpose>

<output>
- apps/frontend/package.json (updated)
- apps/frontend/tsconfig.json (updated)
- apps/frontend/vite.config.ts (updated)
- apps/frontend/src/App.tsx (updated)
- apps/frontend/src/App.test.tsx
- apps/frontend/vitest.config.ts
- apps/frontend/index.html (updated with Tailwind)
- apps/frontend/src/index.css (with Tailwind directives)
</output>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/.planning/PROJECT.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/.planning/ROADMAP.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/.planning/STATE.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/.planning/phases/01-monorepo-foundation/01-CONTEXT.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/.planning/phases/01-monorepo-foundation/01-RESEARCH.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/apps/frontend/package.json
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/apps/frontend/tsconfig.json
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/apps/frontend/vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update frontend package.json with dependencies</name>
  <read_first>
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/apps/frontend/package.json
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/.planning/phases/01-monorepo-foundation/01-CONTEXT.md (D-09, D-12, D-13, D-14)
  </read_first>
  <files>apps/frontend/package.json</files>
  <action>
    Update apps/frontend/package.json to (per D-09, D-12, D-13, D-14):

    ```json
    {
      "name": "@ks0555/frontend",
      "version": "0.1.0",
      "private": true,
      "type": "module",
      "scripts": {
        "dev": "vite",
        "build": "tsc -b && vite build",
        "preview": "vite preview",
        "typecheck": "tsc --noEmit",
        "lint": "eslint . --config ../../packages/eslint-config/src/react.ts",
        "test": "vitest run"
      },
      "dependencies": {
        "react": "^19.2.5",
        "react-dom": "^19.2.5"
      },
      "devDependencies": {
        "@ks0555/eslint-config": "workspace:*",
        "@ks0555/tsconfig": "workspace:*",
        "@types/react": "^19.2.5",
        "@types/react-dom": "^19.2.5",
        "@vitejs/plugin-react": "^4.5.0",
        "typescript": "~5.9.3",
        "vite": "^8.0.8",
        "tailwindcss": "^4.2.2",
        "@tailwindcss/vite": "^4.2.2",
        "vitest": "^4.1.4",
        "@testing-library/react": "^16.3.2",
        "@testing-library/jest-dom": "^6.9.1",
        "jsdom": "^29.0.2"
      }
    }
    ```

    Key changes (per CONTEXT.md decisions):
    - Add "tailwindcss" and "@tailwindcss/vite" to devDependencies (D-13)
    - Add "vitest", "@testing-library/react", "@testing-library/jest-dom", "jsdom" to devDependencies (D-14)
    - Add "@ks0555/eslint-config": "workspace:*" (D-12)
    - Add "@ks0555/tsconfig": "workspace:*" (D-10)
    - Update "lint" script to use shared eslint-config react preset (D-12)
    - Add "test" script (D-14)
    - Update React and Vite versions to match showcase stack (D-09)
  </action>
  <acceptance_criteria>
    - apps/frontend/package.json contains "tailwindcss": "^4.2.2"
    - apps/frontend/package.json contains "vitest": "^4.1.4"
    - apps/frontend/package.json contains "@testing-library/react": "^16.3.2"
    - apps/frontend/package.json contains "@ks0555/eslint-config": "workspace:*"
    - apps/frontend/package.json contains "test": "vitest run"
    - apps/frontend/package.json contains "lint": "eslint . --config ../../packages/eslint-config/src/react.ts"
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"tailwindcss"' apps/frontend/package.json && grep -q '"vitest"' apps/frontend/package.json && grep -q '"@ks0555/eslint-config"' apps/frontend/package.json && echo "Frontend package.json updated correctly"</automated>
  </verify>
  <done>Frontend package.json updated with Tailwind, Vitest, and shared configs</done>
</task>

<task type="auto">
  <name>Task 2: Update frontend tsconfig to extend shared config</name>
  <read_first>
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/apps/frontend/tsconfig.json
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/.planning/phases/01-monorepo-foundation/01-CONTEXT.md (D-10)
  </read_first>
  <files>apps/frontend/tsconfig.json</files>
  <action>
    Update apps/frontend/tsconfig.json to extend shared tsconfig (per D-10):

    ```json
    {
      "extends": "@ks0555/tsconfig/tsconfig.react.json",
      "compilerOptions": {
        "lib": ["ES2022", "DOM", "DOM.Iterable"],
        "jsx": "react-jsx",
        "baseUrl": ".",
        "paths": {
          "@/*": ["./src/*"]
        }
      },
      "include": ["src"],
      "exclude": ["node_modules", "dist"]
    }
    ```

    Key changes:
    - Add "extends": "@ks0555/tsconfig/tsconfig.react.json" (D-10)
    - Add "baseUrl": "." and "paths" for `@/*` alias (D-15)
    - Keep "jsx": "react-jsx" (Vite requires this)
    - Remove redundant compilerOptions already in base config (strict, module, etc.)
  </action>
  <acceptance_criteria>
    - apps/frontend/tsconfig.json contains '"extends": "@ks0555/tsconfig/tsconfig.react.json"'
    - apps/frontend/tsconfig.json contains '"@/*": ["./src/*"]' (D-15)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"extends": "@ks0555/tsconfig/tsconfig.react.json"' apps/frontend/tsconfig.json && grep -q '"@/\*"' apps/frontend/tsconfig.json && echo "Frontend tsconfig extends shared config with aliases"</automated>
  </verify>
  <done>Frontend tsconfig extends shared packages/tsconfig/tsconfig.react.json with path aliases</done>
</task>

<task type="auto">
  <name>Task 3: Update Vite config with Tailwind and path aliases</name>
  <read_first>
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/apps/frontend/vite.config.ts
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/.planning/phases/01-monorepo-foundation/01-CONTEXT.md (D-13, D-15)
  </read_first>
  <files>apps/frontend/vite.config.ts</files>
  <action>
    Update apps/frontend/vite.config.ts to include Tailwind plugin and path aliases (per D-13, D-15):

    ```typescript
    import { defineConfig } from 'vite'
    import react from '@vitejs/plugin-react'
    import tailwindcss from '@tailwindcss/vite'

    export default defineConfig({
      plugins: [react(), tailwindcss()],
      resolve: {
        alias: {
          '@': '/src'
        }
      }
    })
    ```

    Key changes:
    - Import and add `tailwindcss()` plugin (D-13)
    - Add `resolve.alias` with `@` mapping to `/src` (D-15)
  </action>
  <acceptance_criteria>
    - apps/frontend/vite.config.ts contains "import tailwindcss from '@tailwindcss/vite'"
    - apps/frontend/vite.config.ts contains "tailwindcss()" in plugins array
    - apps/frontend/vite.config.ts contains "alias: { '@': '/src' }" (D-15)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'tailwindcss' apps/frontend/vite.config.ts && grep -q '"@":' apps/frontend/vite.config.ts && echo "Vite config has Tailwind plugin and path aliases"</automated>
  </verify>
  <done>Vite config configured with Tailwind CSS plugin and @ path alias</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create App component, test, and Tailwind styles</name>
  <read_first>
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/.planning/phases/01-monorepo-foundation/01-CONTEXT.md (D-11, D-14)
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/apps/frontend/index.html
  </read_first>
  <files>apps/frontend/src/App.tsx, apps/frontend/src/App.test.tsx, apps/frontend/src/index.css, apps/frontend/index.html</files>
  <behavior>
    - Test 1: App component renders without crashing
    - Test 2: App displays "Hello from Steam Deck Controller"
    - Test 3: Tailwind classes are applied (text-xl class exists)
  </behavior>
  <action>
    1. Update apps/frontend/index.html to include Tailwind:

    ```html
    <!DOCTYPE html>
    <html lang="en">
      <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Steam Deck Robot Controller</title>
      </head>
      <body>
        <div id="root"></div>
        <script type="module" src="/src/main.tsx"></script>
      </body>
    </html>
    ```

    2. Create apps/frontend/src/index.css with Tailwind directives:

    ```css
    @import "tailwindcss";
    ```

    3. Create/update apps/frontend/src/App.tsx (per D-11):

    ```tsx
    export function App() {
      return (
        <div class Name="min-h-screen bg-gray-100 flex items-center justify-center">
          <h1 class Name="text-xl font-bold text-gray-800">
            Hello from Steam Deck Controller
          </h1>
        </div>
      )
    }
    ```

    4. Create apps/frontend/src/App.test.tsx (per D-14):

    ```tsx
    import { describe, it, expect } from 'vitest'
    import { render, screen } from '@testing-library/react'
    import '@testing-library/jest-dom'
    import { App } from './App'

    describe('App', () => {
      it('renders hello message', () => {
        render(<App />)
        expect(screen.getByText('Hello from Steam Deck Controller')).toBeInTheDocument()
      })
    })
    ```

    5. Create apps/frontend/vitest.config.ts:

    ```typescript
    import { defineConfig } from 'vitest/config'
    import react from '@vitejs/plugin-react'

    export default defineConfig({
      plugins: [react()],
      test: {
        environment: 'jsdom',
        include: ['src/**/*.test.{ts,tsx}']
      }
    })
    ```

    6. Create apps/frontend/src/main.tsx (entry point):

    ```tsx
    import React from 'react'
    import ReactDOM from 'react-dom/client'
    import App from './App'
    import './index.css'

    ReactDOM.createRoot(document.getElementById('root')!).render(
      <React.StrictMode>
        <App />
      </React.StrictMode>
    )
    ```
  </action>
  <acceptance_criteria>
    - apps/frontend/src/App.tsx exists and contains "Hello from Steam Deck Controller"
    - apps/frontend/src/App.test.tsx exists with at least one test
    - apps/frontend/src/index.css contains "@import "tailwindcss""
    - apps/frontend/vitest.config.ts exists with jsdom environment
    - `cd apps/frontend && vitest run` passes (or `pnpm --filter frontend test`)
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/frontend/src/App.tsx && test -f apps/frontend/src/App.test.tsx && grep -q 'tailwindcss' apps/frontend/src/index.css && echo "Frontend App and tests created"</automated>
  </verify>
  <done>Frontend App component created with Tailwind CSS and Vitest tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→frontend | Browser to Vite dev server (port 5173) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Spoofing | Frontend dev server | accept | Local development only, no auth needed for MVP |
| T-01-09 | Tampering | Vite HMR websocket | accept | Localhost only, no security risk for development |
| T-01-10 | Information Disclosure | Source maps | accept | Dev only, source maps help debugging |
| T-01-11 | Denial of Service | Vite dev server | accept | Single-user local device, no DoS risk |
| T-01-12 | Elevation of Privilege | N/A | accept | No auth system for MVP |
</threat_model>

<verification>
- `cd apps/frontend && grep -q '"tailwindcss"' package.json`
- `cd apps/frontend && grep -q '"extends": "@ks0555/tsconfig/tsconfig.react.json"' tsconfig.json`
- `cd apps/frontend && grep -q 'tailwindcss()' vite.config.ts`
- `cd apps/frontend && vitest run` passes (or `pnpm --filter frontend test`)
- `pnpm typecheck` passes for frontend
</verification>

<success_criteria>
- [ ] Frontend has Vite + React + TypeScript working (D-09, D-10)
- [ ] Tailwind CSS configured with @tailwindcss/vite plugin (D-13)
- [ ] Vitest + @testing-library/react configured with passing test (D-14)
- [ ] Path alias `@` maps to `src/` in tsconfig and vite config (D-15)
- [ ] ESLint extends shared packages/eslint-config react preset (D-12)
- [ ] `pnpm --filter frontend dev` starts Vite dev server
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-foundation/01-03-SUMMARY.md`
</output>
