---
phase: 16-appimage-decommission-upgrade-workflow-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - apps/frontend/src-tauri/ARCHITECTURE.md
  - flatpak/README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DECK-05
must_haves:
  truths:
    - "Root README install section describes Flatpak-based install, not AppImage"
    - "ARCHITECTURE.md exists and documents the full system: build chain, sandbox model, D-Bus gate, event pipeline, monorepo layout"
    - "flatpak/README.md includes finish-args rationale, D-Bus gate explanation, and anti-feature checklist"
  artifacts:
    - path: "README.md"
      provides: "User-facing install instructions with Flatpak walkthrough"
      contains: "flatpak install --user"
    - path: "apps/frontend/src-tauri/ARCHITECTURE.md"
      provides: "Full system architecture documentation"
      min_lines: 80
    - path: "flatpak/README.md"
      provides: "Updated contributor guide with sandbox rationale"
      contains: "finish-args"
  key_links:
    - from: "README.md"
      to: "upgrade-robot-controller.sh"
      via: "text reference"
      pattern: "upgrade-robot-controller\\.sh"
    - from: "apps/frontend/src-tauri/ARCHITECTURE.md"
      to: "flatpak/README.md"
      via: "doc cross-reference"
      pattern: "flatpak/README\\.md"
    - from: "apps/frontend/src-tauri/ARCHITECTURE.md"
      to: "apps/frontend/src-tauri/src/lib.rs"
      via: "code reference for in_flatpak()"
      pattern: "in_flatpak"
---

<objective>

<objective>
Rewrite the root README to replace all AppImage-based install instructions with Flatpak equivalents. Create a comprehensive ARCHITECTURE.md documenting the full system. Update flatpak/README.md with sandbox permission rationale added in Phases 12-13.

Purpose: Onboard new users with correct Flatpak instructions. Document the system architecture so contributors understand the deb-extract build chain, Flatpak sandbox model, D-Bus gate pattern, and Tauri event pipeline.

Output: Updated README.md, new ARCHITECTURE.md, updated flatpak/README.md.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-CONTEXT.md

<interfaces>
<!-- Key files referenced by this plan -->

From README.md (current state — AppImage-based):
- Lines 9-31: "Install on Steam Deck" section uses `curl ... install-on-steamdeck.sh | bash` and AppImage manual install
- Lines 97-113: Build from Source section mentions `.github/workflows/build.yml` produces deb
- Line 112: Project Layout table lists `install-on-steamdeck.sh`

From flatpak/README.md (current state):
- Lines 62-74: Architecture section describes deb-extract pattern
- Lines 76-86: Flatpak Manifest section — finish-args listed as "display only" with note that BLE/gamepad args "are added in a later phase"
- Lines 118-125: Checklist Coverage maps DECK-01 through VAL-09

From flatpak/com.ks0555.robotcontroller.yaml (read-only reference):
- finish-args include: --socket=wayland, --socket=fallback-x11, --share=ipc, --device=dri, --env=WEBKIT_DISABLE_COMPOSITING_MODE=1, --system-talk-name=org.bluez, --system-talk-name=org.bluez.*, --allow=bluetooth, --share=network, --device=input
- Anti-feature checklist comment at top (lines 1-8)

From apps/frontend/src-tauri/src/lib.rs (read-only reference):
- in_flatpak() function: checks FLATPAK_ID env + `/.flatpak-info` file existence (belt-and-suspenders)
- D-Bus rewrite block gated behind `!in_flatpak()`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite root README.md install section for Flatpak</name>
  <read_first>
    - README.md — understand current structure to preserve sections that don't change
    - .planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-CONTEXT.md — D-22 for README rewrite decisions
    - .planning/phases/14-steam-deck-on-device-validation/14-CONTEXT.md — Gaming Mode workflow details (for walkthrough text)
  </read_first>
  <files>README.md</files>
  <action>
Rewrite the README.md `## Install on Steam Deck` section (lines 9-31) and `### Manual install` subsection per D-22. Keep all other sections (Architecture, Commands, Gamepad Mapping, Build from Source, Project Layout, Development Scripts, Technology Stack, License) unchanged except where they contain stale AppImage references.

**Replace "Install on Steam Deck" section (currently lines 9-23):**
Replace the `curl -fsSL ... install-on-steamdeck.sh | bash` block and its 3-step description with Flatpak-based instructions:

```markdown
## Install on Steam Deck

### Quick install

In **Desktop Mode**, open Konsole and run:

```bash
curl -fsSL https://raw.githubusercontent.com/pau-vega/KS0555-Steam-Deck-Controller-2/main/upgrade-robot-controller.sh | bash
```

That script:
1. Checks if Robot Controller is already installed via Flatpak. If not, downloads and installs the latest `.flatpak` from GitHub Releases.
2. If already installed, checks for a newer version and offers to upgrade.
3. Displays the release changelog so you can see what changed.

Final manual step: in Steam → Library → **+** → **Add a Non-Steam Game** → pick **com.ks0555.robotcontroller** → _Add Selected Programs_. Switch to Gaming Mode and it lives under the Non-Steam tab.

### Manual install (no script)

If you'd rather not pipe to bash:
1. Download `RobotController-x86_64.flatpak` from the [latest release](https://github.com/pau-vega/KS0555-Steam-Deck-Controller-2/releases/latest).
2. `flatpak install --user ~/Downloads/RobotController-x86_64.flatpak`
3. Steam → Library → **+** → **Add a Non-Steam Game** → pick **com.ks0555.robotcontroller** → _Add Selected Programs_.

### Upgrading

Re-run the curl line above, or:
```bash
./upgrade-robot-controller.sh --check   # just check for updates
./upgrade-robot-controller.sh           # check and install
```
```

**Update Build from Source section (currently lines 94-102):**
- Line 101: Replace "The GitHub Actions workflow at `.github/workflows/build.yml` produces the deb attached to each tagged release." with "The GitHub Actions workflow at `.github/workflows/build.yml` produces the `.flatpak` attached to each tagged release."
- Add note: "The `.flatpak` is a single-file bundle — no runtime download needed on first install (Flathub remote auto-fetches `org.freedesktop.Platform//24.08`)."

**Update Project Layout table (currently lines 106-113):**
- Remove row: `| install-on-steamdeck.sh | One-shot installer for end users |`
- Add row: `| upgrade-robot-controller.sh | Upgrade/install launcher for Steam Deck users |`

**Update Run on Mac section (lines 33-40):**
- Keep unchanged — macOS DMG distribution is still valid.

**Verify no stale references remain:**
- `grep -c 'AppImage' README.md` must be 0
- `grep -c 'install-on-steamdeck.sh' README.md` must be 0
- `grep -c 'flatpak install --user' README.md` must be at least 2
- `grep -c 'upgrade-robot-controller.sh' README.md` must be at least 1
- `grep -c 'com.ks0555.robotcontroller' README.md` must be at least 2

**Preserve:**
- Architecture section (lines 63-68, ASCII diagram)
- Commands table (lines 72-80)
- Gamepad Mapping table (lines 83-92)
- Development Scripts table (lines 118-125)
- Technology Stack list (lines 128-138)
- License (lines 140-141)
- The `<!-- generated-by: gsd-doc-writer -->` comment at top
</action>
  <verify>
    <automated>grep -c 'AppImage' README.md | grep '^0$' && grep -c 'install-on-steamdeck.sh' README.md | grep '^0$' && grep -c 'flatpak install --user' README.md | grep -v '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AppImage' README.md` returns 0 (D-09: full AppImage cleanup)
    - `grep -c 'install-on-steamdeck.sh' README.md` returns 0 (D-08: no references to deleted script)
    - `grep -c 'flatpak install --user' README.md` returns at least 2 (Flatpak install instructions exist)
    - `grep -c 'upgrade-robot-controller.sh' README.md` returns at least 1 (launcher script referenced)
    - `grep -c 'com.ks0555.robotcontroller' README.md` returns at least 2 (Flatpak app ID used in instructions)
    - `grep -c 'RobotController-x86_64.flatpak' README.md` returns at least 1 (Flatpak bundle referenced)
    - `grep "Add a Non-Steam Game" README.md` returns at least 1 (Gaming Mode setup preserved)
    - `grep -c 'flatpak run com.ks0555.robotcontroller' README.md` is 0 or 1 (run command optionally mentioned)
    - `grep "Build and Release" README.md` returns 1 (CI reference updated to say .flatpak, not .deb)
    - `grep "<!-- generated-by: gsd-doc-writer -->" README.md` returns 1 (header preserved)
  </acceptance_criteria>
  <done>Root README install section rewritten for Flatpak. Zero AppImage references. Upgrade launcher linked. Project Layout table updated.</done>
</task>

<task type="auto">
  <name>Task 2: Create apps/frontend/src-tauri/ARCHITECTURE.md</name>
  <read_first>
    - flatpak/com.ks0555.robotcontroller.yaml — manifest structure, finish-args, anti-feature checklist, deb-extract build-commands
    - flatpak/com.ks0555.robotcontroller.metainfo.xml — AppStream metainfo
    - apps/frontend/src-tauri/src/lib.rs — in_flatpak() function, D-Bus gate pattern
    - apps/frontend/src-tauri/src/ble/mod.rs — BLE module (for event pipeline docs)
    - apps/frontend/src-tauri/src/gamepad/mod.rs — gamepad module (for event pipeline docs)
    - .planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-CONTEXT.md — D-20, D-21 architecture doc decisions
    - .planning/phases/13-sandbox-permissions-ble-gamepad/13-CONTEXT.md — D-01 (belt-and-suspenders), D-02 (D-Bus gate scope), D-05 (anti-feature checklist)
  </read_first>
  <files>apps/frontend/src-tauri/ARCHITECTURE.md</files>
  <action>
Create `apps/frontend/src-tauri/ARCHITECTURE.md` — a comprehensive system architecture document. Per D-20, cover these sections in order:

**Structure (per CONTEXT.md specifics line 141):**

```markdown
# Robot Controller — System Architecture

## Overview

Single Tauri v2 desktop application. Rust backend owns BLE (`btleplug`) and gamepad (`gilrs`). React frontend communicates via Tauri IPC (`invoke` / `listen`). Packaged as a Flatpak for Steam Deck distribution. No separate backend process, no `rfcomm`, no Chrome flags.

## Build Chain

1. **Rust + Tauri → .deb**: `cargo tauri build --bundles deb` compiles the Rust binary, bundles the Vite-built React frontend, and packages everything into a Debian package.
   - Binary: `apps/frontend/src-tauri/target/release/bundle/deb/robot-controller_*.deb`
   - Tauri CLI v2 (stock from crates.io, no fork)
   - Frontend built via `pnpm build` (Vite + React + TypeScript)

2. **.deb → Flatpak**: The Flatpak manifest (`flatpak/com.ks0555.robotcontroller.yaml`) uses a `type: file` source to ingest the `.deb`, then extracts it with `ar -x` + `tar -xf`. Build-commands install the binary, desktop file, icons, and metainfo to Flatpak-standard paths under `/app/`.

## Flatpak Packaging

### Manifest

- **Location:** `flatpak/com.ks0555.robotcontroller.yaml`
- **Runtime:** `org.freedesktop.Platform//24.08`
- **SDK:** `org.freedesktop.Sdk//24.08`
- **Extension:** `org.freedesktop.Platform.GL.default`
- **Command:** `robot-controller` (the binary name inside the sandbox)

### Deb-Extract Pattern

The manifest uses Flatpak's `buildsystem: simple` with inline shell commands:
- `ar -x robot-controller.deb` — extract the Debian archive
- `tar -xf data.tar.*` — unpack the filesystem tree
- `install -Dm755` / `install -Dm644` — place files in Flatpak paths
- Desktop file is renamed to `com.ks0555.robotcontroller.desktop` and its `Icon=` line is updated

### AppStream Metainfo

- **Location:** `flatpak/com.ks0555.robotcontroller.metainfo.xml`
- Provides application metadata (name, summary, description, screenshots, categories) for software centers and the desktop shell.

## Sandbox Model

Flatpak runs the application in a container with strictly controlled access to host resources. Every permission is explicitly requested via `finish-args` in the manifest.

### Display

| finish-arg | Purpose |
|------------|---------|
| `--socket=wayland` | Native Wayland rendering on Steam Deck (Gamescope) |
| `--socket=fallback-x11` | X11 fallback for non-Wayland desktops |
| `--share=ipc` | Shared memory for X11/Wayland performance |
| `--device=dri` | Direct Rendering Infrastructure for GPU access |
| `--env=WEBKIT_DISABLE_COMPOSITING_MODE=1` | Disable WebKit compositing bypass (belt-and-suspenders: also set in lib.rs and CI) |

### BLE (Bluetooth Low Energy)

| finish-arg | Purpose |
|------------|---------|
| `--system-talk-name=org.bluez` | Allow D-Bus calls to BlueZ on the system bus (btleplug uses D-Bus, not AF_BLUETOOTH) |
| `--system-talk-name=org.bluez.*` | Wildcard for BlueZ sub-interfaces (adapter, device, GATT) |
| `--allow=bluetooth` | AF_BLUETOOTH socket permission (supplementary; btleplug may use it on some platforms) |
| `--share=network` | Network access (BlueZ D-Bus sometimes requires it for discovery) |

### Gamepad

| finish-arg | Purpose |
|------------|---------|
| `--device=input` | Read access to `/dev/input/event*` for gilrs evdev gamepad detection |

**Requirement:** Flatpak ≥ 1.15.6 for `--device=input`. SteamOS 3.6+ ships Flatpak 1.15.8+.

### Anti-Feature Checklist

The manifest includes a comment block at the top listing permissions that are deliberately NOT requested:
- `--filesystem=home` — Unnecessary sandbox escape
- `--device=bluetooth` — Wrong stack (AF_BLUETOOTH not D-Bus)
- `--talk-name=org.bluez` — Wrong bus (session, not system)
- `--socket=session-bus` — Not needed for this app
- `--socket=system-bus` — Over-broad; `--system-talk-name` is more targeted (principle of least privilege)

## D-Bus Gate (`in_flatpak()`)

When running inside a Flatpak sandbox, the application must NOT attempt to rewrite the D-Bus system bus socket or set `DBUS_SYSTEM_BUS_ADDRESS` — Flatpak provides its own D-Bus proxy.

### Detection (belt-and-suspenders)

```rust
// apps/frontend/src-tauri/src/lib.rs
fn in_flatpak() -> bool {
    std::env::var("FLATPAK_ID").is_ok() || std::path::Path::new("/.flatpak-info").exists()
}
```

Two independent checks:
1. `FLATPAK_ID` environment variable — set by Flatpak to the app ID (`com.ks0555.robotcontroller`)
2. `/.flatpak-info` file — always present inside a Flatpak sandbox

### Gate Logic

The entire D-Bus rewrite block (SteamOS detection, Gamescope socket path, `DBUS_SYSTEM_BUS_ADDRESS` override) is gated behind `!in_flatpak()`. When running as a Flatpak:
- Flatpak's D-Bus proxy handles all `org.bluez` communication transparently
- Setting `DBUS_SYSTEM_BUS_ADDRESS` would break the proxy and cause BLE to fail silently

### Non-Flatpak Behavior

When NOT in Flatpak (native Linux, macOS):
- SteamOS detection: checks for `WEBKIT_DISABLE_COMPOSITING_MODE=1` env var
- Sets `DBUS_SYSTEM_BUS_ADDRESS` to Gamescope's system bus socket (`/run/host/run/dbus/system_bus_socket`)
- On macOS: `btleplug` uses CoreBluetooth (no D-Bus), `gilrs` uses IOKit (no evdev)

## Event Pipeline

### BLE Communication

```
React (use-bluetooth.ts)
  │ invoke("ble_connect") / invoke("ble_send", { command })
  ▼
Tauri IPC
  │ #[tauri::command]
  ▼
ble::connect() / ble::send()
  │ btleplug (BlueZ D-Bus on Linux, CoreBluetooth on macOS)
  ▼
BT24 Module ──UART──▶ Arduino
```

Events flow back through `emit("ble-state-changed", ...)`:
```
btleplug CentralEvent
  │ DeviceConnected / DeviceDisconnected
  ▼
ble::event_loop()
  │ app_handle.emit("ble-state-changed", payload)
  ▼
Tauri IPC
  │ listen("ble-state-changed", ...)
  ▼
React (use-bluetooth.ts)
```

### Gamepad Input

```
gilrs event loop (background thread)
  │ gilrs::next_event()
  ▼
Direction detection: Axis::LeftStickX/Y → deadzone (0.15) → F/B/L/R/S
  │ direction change guard (only emit on change)
  ▼
app_handle.emit("gamepad-direction", direction)
  │
  ▼
Tauri IPC
  │ listen("gamepad-direction", ...)
  ▼
React (use-gamepad.ts)
```

Connect/disconnect events:
```
gilrs::EventType::Connected → emit("gamepad-connected")
gilrs::EventType::Disconnected → emit("gamepad-disconnected")
```

Steam Deck detection: `gamepad.name().contains("Steam")` — prefers the built-in controller over external gamepads.

## Frontend Integration

### React Hooks

- **`use-bluetooth.ts`**: Calls `invoke("ble_connect")`, `invoke("ble_disconnect")`, `invoke("ble_send", { command })`. Listens to `listen("ble-state-changed")` for state updates. Return shape: `{ connected, connecting, unsupported, connect, send }`.
- **`use-gamepad.ts`**: Listens to `listen("gamepad-direction")`, `listen("gamepad-connected")`, `listen("gamepad-disconnected")`. Return shape: `{ direction, gamepadConnected }`.

### Tauri Commands

| Command | Signature | Purpose |
|---------|-----------|---------|
| `ble_connect` | `() -> Result<(), String>` | Scan for BT24, connect, emit state |
| `ble_disconnect` | `() -> Result<(), String>` | Disconnect BT24 peripheral |
| `ble_send` | `(command: String) -> Result<(), String>` | Write F/B/L/R/S to BT24 characteristic |
| `get_ble_state` | `() -> String` | Get current BLE connection state |

### Vite Integration

- Dev server on fixed port 5173 (matches `devUrl` in `tauri.conf.json`)
- `vite.config.ts`: `clearScreen: false`, `strictPort: true`, watch ignores `src-tauri/`

## Monorepo Layout

```
apps/frontend/                     # Tauri app
├── src/
│   ├── app.tsx                    # App entry (VAL-08: locked, no edits)
│   ├── main.tsx                   # React mount
│   ├── components/
│   │   ├── control-pad.tsx        # Direction pad (VAL-08: locked)
│   │   └── status-bar.tsx         # Connection status (VAL-08: locked)
│   └── hooks/
│       ├── use-bluetooth.ts       # BLE state hook
│       └── use-gamepad.ts         # Gamepad direction hook
├── src-tauri/
│   ├── Cargo.toml                 # Rust dependencies + version
│   ├── tauri.conf.json            # Tauri window/bundle config
│   ├── icons/                     # App icons (all platforms)
│   ├── src/
│   │   ├── main.rs                # Tauri entrypoint
│   │   ├── lib.rs                 # Setup hook, in_flatpak(), D-Bus gate
│   │   ├── ble/mod.rs             # BLE scan/connect/send via btleplug
│   │   └── gamepad/mod.rs         # Gamepad polling via gilrs
│   ├── Info.plist                 # macOS Bluetooth permission description
│   └── ARCHITECTURE.md            # This file
├── index.html                     # Vite HTML entry
├── vite.config.ts                 # Vite + Tauri integration
└── package.json                   # Frontend deps + scripts
flatpak/
├── com.ks0555.robotcontroller.yaml    # Flatpak manifest
├── com.ks0555.robotcontroller.metainfo.xml  # AppStream metadata
├── build.sh                       # Local Flatpak build script
├── README.md                      # Flatpak contributor guide
├── VALIDATION-CHECKLIST.md        # On-device validation checklist
├── validation-reports/            # Dated validation reports (git-ignored)
├── validation-logs/               # Captured log files (git-ignored)
└── icons/                         # Flatpak icon files (32/128/256@2)
packages/
├── eslint-config/                 # Shared ESLint flat config
└── tsconfig/                      # Shared TypeScript config
.planning/                         # GSD planning artifacts (git-ignored in PR)
```

## Platform Support

| Platform | BLE | Gamepad | Distribution |
|----------|-----|---------|-------------|
| Steam Deck (SteamOS) | BlueZ D-Bus (via Flatpak proxy) | gilrs evdev (via `--device=input`) | `.flatpak` bundle |
| Linux (native) | BlueZ D-Bus (direct) | gilrs evdev (via udev) | `.deb` or source |
| macOS | CoreBluetooth | gilrs IOKit | `.dmg` |
| Windows | Not supported | — | — |

## Key Decisions

- **Flatpak runtime locked:** `org.freedesktop.Platform//24.08` (chosen in Phase 11, validated in Phase 14)
- **Sideload-only distribution:** `.flatpak` bundles from GitHub Releases — no Flathub publication, no OSTree remote. `flatpak install --user --reinstall` for upgrades.
- **Belt-and-suspenders Flatpak detection:** Both `FLATPAK_ID` env var AND `/.flatpak-info` file checked before D-Bus rewrite.
- **Single-binary:** One Tauri process, no separate backend. BLE and gamepad run in the same Rust binary.
- **app.tsx locked:** CI and pre-commit hooks enforce `git diff --exit-code` on `app.tsx`, `control-pad.tsx`, `status-bar.tsx`.

## References

- [flatpak/README.md](../../flatpak/README.md) — Flatpak build, install, and run guide
- [flatpak/com.ks0555.robotcontroller.yaml](../../flatpak/com.ks0555.robotcontroller.yaml) — Flatpak manifest
- [lib.rs](src/lib.rs) — `in_flatpak()` detection and D-Bus gate
- [ble/mod.rs](src/ble/mod.rs) — BLE implementation
- [gamepad/mod.rs](src/gamepad/mod.rs) — Gamepad implementation
- [Tauri v2 Documentation](https://v2.tauri.app/)
- [btleplug](https://github.com/deviceplug/btleplug)
- [gilrs](https://docs.rs/gilrs/)
- [Flatpak Documentation](https://docs.flatpak.org/)
```

Write the above content to `apps/frontend/src-tauri/ARCHITECTURE.md`.
</action>
  <verify>
    <automated>test -f apps/frontend/src-tauri/ARCHITECTURE.md && wc -l < apps/frontend/src-tauri/ARCHITECTURE.md | awk '{if($1>=80) exit 0; else exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/frontend/src-tauri/ARCHITECTURE.md` returns 0 (file exists per D-20)
    - File has at least 80 lines of substantive content
    - `grep -c "in_flatpak" apps/frontend/src-tauri/ARCHITECTURE.md` is at least 2 (D-Bus gate documented)
    - `grep -c "deb-extract" apps/frontend/src-tauri/ARCHITECTURE.md` is at least 1 (build chain documented)
    - `grep -c "finish-args" apps/frontend/src-tauri/ARCHITECTURE.md` is at least 1 (sandbox model documented)
    - `grep -c "belt-and-suspenders" apps/frontend/src-tauri/ARCHITECTURE.md` is at least 1 (Phase 13 D-01 pattern documented)
    - `grep -c "system-talk-name=org.bluez" apps/frontend/src-tauri/ARCHITECTURE.md` is at least 1 (BLE sandbox args documented)
    - `grep -c "device=input" apps/frontend/src-tauri/ARCHITECTURE.md` is at least 1 (gamepad sandbox args documented)
    - `grep -c "Anti-Feature Checklist" apps/frontend/src-tauri/ARCHITECTURE.md` is at least 1 (D-05 checklist documented)
    - `grep -c "flatpak/README.md" apps/frontend/src-tauri/ARCHITECTURE.md` is at least 1 (cross-reference to flatpak README per D-21)
    - `grep -c "lib.rs" apps/frontend/src-tauri/ARCHITECTURE.md` is at least 1 (code reference to D-Bus gate)
    - `grep -c "## Key Decisions" apps/frontend/src-tauri/ARCHITECTURE.md` returns 1
    - `grep "BT24 Module" apps/frontend/src-tauri/ARCHITECTURE.md` returns 1 (event pipeline ASCII diagram present)
    - `grep -c "VAL-08" apps/frontend/src-tauri/ARCHITECTURE.md` is at least 1 (app.tsx lock noted)
  </acceptance_criteria>
  <done>ARCHITECTURE.md created with full coverage: build chain, Flatpak packaging, sandbox model with finish-args rationale, D-Bus gate with belt-and-suspenders explanation, event pipeline diagrams, monorepo layout, platform support matrix, and key decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Update flatpak/README.md with sandbox rationale</name>
  <read_first>
    - flatpak/README.md — understand current content before editing
    - flatpak/com.ks0555.robotcontroller.yaml — actual finish-args to document
    - .planning/phases/13-sandbox-permissions-ble-gamepad/13-CONTEXT.md — D-01 (belt-and-suspenders), D-05 (anti-feature checklist)
    - .planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-CONTEXT.md — D-21 (flatpak/README.md update decisions)
  </read_first>
  <files>flatpak/README.md</files>
  <action>
Update `flatpak/README.md` per D-21. The file currently describes finish-args as "display only" (line 82-86) with a note that BLE/gamepad args "are added in a later phase." That later phase (13) is now complete — update to reflect current state.

**Replace the "Flatpak Manifest" section (lines 76-86):**

Replace the existing finish-args list and the note about "later phase" with a comprehensive finish-args table organized by category:

```markdown
## Flatpak Manifest

- **Location:** `flatpak/com.ks0555.robotcontroller.yaml`
- **Runtime:** `org.freedesktop.Platform//24.08`
- **SDK:** `org.freedesktop.Sdk//24.08`
- **Extension:** `org.freedesktop.Platform.GL.default`

### Finish-args (Sandbox Permissions)

Every `finish-arg` grants the Flatpak sandbox access to a specific host resource. These are grouped by subsystem:

#### Display & Graphics

| finish-arg | Purpose |
|------------|---------|
| `--socket=wayland` | Native Wayland rendering (Gamescope on Steam Deck) |
| `--socket=fallback-x11` | X11 fallback for non-Wayland desktops |
| `--share=ipc` | Shared memory for X11/Wayland IPC performance |
| `--device=dri` | Direct Rendering Infrastructure — GPU access |
| `--env=WEBKIT_DISABLE_COMPOSITING_MODE=1` | Disable WebKit compositing bypass on Steam Deck Gamescope |

#### BLE — Bluetooth Low Energy

| finish-arg | Purpose |
|------------|---------|
| `--system-talk-name=org.bluez` | D-Bus system bus access to BlueZ (btleplug uses D-Bus, not AF_BLUETOOTH) |
| `--system-talk-name=org.bluez.*` | Wildcard for BlueZ sub-interfaces (adapter, device, GATT) |
| `--allow=bluetooth` | AF_BLUETOOTH socket permission (supplementary) |
| `--share=network` | Network access (BlueZ D-Bus sometimes requires it) |

> **Why `--system-talk-name`, not `--socket=system-bus`?** Principle of least privilege. `--socket=system-bus` grants access to _all_ system D-Bus services. `--system-talk-name=org.bluez` grants access to BlueZ only.

> **Why `--allow=bluetooth` in addition to D-Bus?** Belt-and-suspenders. `btleplug` may use AF_BLUETOOTH sockets on some platforms alongside D-Bus. This arg is harmless if unused.

#### Gamepad

| finish-arg | Purpose |
|------------|---------|
| `--device=input` | Read access to `/dev/input/event*` — gilrs reads gamepad events via evdev |

> **Requirement:** Flatpak ≥ 1.15.6 for `--device=input`. SteamOS 3.6+ ships Flatpak 1.15.8+. If `--device=input` is unavailable (older Flatpak), use `--device=all` as a fallback.

### Anti-Feature Checklist

The manifest includes a comment block listing permissions that are deliberately **NOT** requested:

| Rejected Arg | Why It Was Rejected |
|-------------|-------------------|
| `--filesystem=home` | Unnecessary sandbox escape — the app doesn't need filesystem access |
| `--device=bluetooth` | Wrong stack — AF_BLUETOOTH is not D-Bus; btleplug talks BlueZ over D-Bus |
| `--talk-name=org.bluez` | Wrong bus — this is session bus, not system bus; need `--system-talk-name` |
| `--socket=session-bus` | Tray icon arg, not needed for this app |
| `--socket=system-bus` | Over-broad — grants all system D-Bus; `--system-talk-name=org.bluez` is more targeted |
| org.freedesktop.Flatpak | Portal grant, not needed (no portal interaction) |

### D-Bus Gate (`in_flatpak()`)

When running inside the Flatpak sandbox, the application detects it via a belt-and-suspenders check:
1. `FLATPAK_ID` environment variable is set
2. `/.flatpak-info` file exists

If either is true → `in_flatpak()` returns `true` → the D-Bus system bus socket rewrite is **skipped**. Flatpak's D-Bus proxy handles `org.bluez` communication transparently. Rewriting `DBUS_SYSTEM_BUS_ADDRESS` inside the sandbox would break the proxy and cause BLE to fail silently.

The detection logic lives in `apps/frontend/src-tauri/src/lib.rs`:
```rust
fn in_flatpak() -> bool {
    std::env::var("FLATPAK_ID").is_ok() || std::path::Path::new("/.flatpak-info").exists()
}
```

### Deb-Extract Pattern

The manifest uses a `type: file` source to ingest the `.deb` produced by `cargo tauri build --bundles deb`, extracts it with `ar -x` + `tar -xf`, and installs files to Flatpak-standard paths under `/app/`.

See [ARCHITECTURE.md](../apps/frontend/src-tauri/ARCHITECTURE.md) for full system architecture documentation.
```

**Update the "Quick Start" section (lines 93-104):**
- Line 98: Change `./flatpak/build.sh apps/frontend/src-tauri/target/release/bundle/deb/robot-controller_*.deb` to note that `build.sh` is the local-build script. CI uses flatpak-builder directly (not build.sh).
- Add reference: "For CI builds, see `.github/workflows/build.yml` — the CI runs flatpak-builder directly using `flatpak/flatpak-github-actions/flatpak-builder@v6`."

**Update the "Building" section (lines 16-24):**
- The `./flatpak/build.sh` reference stays — it's the local build path
- Add note after step 2: "Note: `build.sh` is the **local** build script. CI uses `flatpak-builder` directly via the GitHub Action (see `.github/workflows/build.yml`). The justfile `flatpak-build` recipe (in root `justfile`) also invokes `flatpak-builder` directly, not `build.sh`."

**Update "Checklist Coverage" table (lines 121-125):**
- Add row: `DECK-05: upgrade-robot-controller.sh polling launcher for version-check + reinstall`
</action>
  <verify>
    <automated>grep -c 'finish-args' flatpak/README.md | grep -v '^0$' && grep -c 'system-talk-name=org.bluez' flatpak/README.md | grep -v '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "finish-args" flatpak/README.md` returns at least 3 (D-21: finish-args covered extensively)
    - `grep -c "system-talk-name=org.bluez" flatpak/README.md` returns at least 1 (BLE sandbox args documented)
    - `grep -c "device=input" flatpak/README.md` returns at least 1 (gamepad sandbox args documented)
    - `grep -c "Anti-Feature Checklist" flatpak/README.md` returns at least 1 (anti-feature checklist present per D-05)
    - `grep -c "D-Bus Gate" flatpak/README.md` returns at least 1 (D-21: in_flatpak gate documented)
    - `grep -c "belt-and-suspenders" flatpak/README.md` returns at least 1 (Phase 13 D-01 pattern documented)
    - `grep -c "principle of least privilege" flatpak/README.md` returns at least 1 (rationale for --system-talk-name over --socket=system-bus)
    - `grep -c "ARCHITECTURE.md" flatpak/README.md` returns at least 1 (cross-reference to new architecture doc)
    - `grep "added in a later phase" flatpak/README.md` returns 0 (stale note removed — all phases complete)
    - `grep -c "Flatpak 1.15.6" flatpak/README.md` returns at least 1 (device=input minimum version noted)
    - `grep -c "FLATPAK_ID" flatpak/README.md` returns at least 1 (in_flatpak detection code shown)
    - `grep -c "DECK-05" flatpak/README.md` returns at least 1 (checklist coverage updated)
    - `grep -c "deb-extract" flatpak/README.md` returns at least 1 (deb-extract pattern still documented)
  </acceptance_criteria>
  <done>flatpak/README.md updated: finish-args grouped by subsystem (display/BLE/gamepad), anti-feature checklist with reasons, D-Bus gate with in_flatpak() code, cross-reference to ARCHITECTURE.md. No stale "later phase" notes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User reading README → GitHub Releases download | User trusts GitHub Releases to serve the correct .flatpak |
| flatpak-builder → Flathub runtime | flatpak-builder fetches org.freedesktop.Platform from Flathub during build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Spoofing | README.md install instructions | mitigate | README points to official GitHub Releases URL; `upgrade-robot-controller.sh` verifies `.sha256` before install (D-18) |
| T-16-06 | Tampering | flatpak-builder runtime fetch | accept | Flathub runtime is fetched over HTTPS with GPG signature verification by flatpak-builder. Risk accepted as flatpak-builder enforces this. |
| T-16-07 | Information Disclosure | ARCHITECTURE.md contains internal paths | accept | Architecture docs are for contributors. No secrets, no credentials, no API keys. Internal paths (e.g., `/app/bin/robot-controller`) are part of the Flatpak spec — publicly documented. |
</threat_model>

<verification>
- `grep -c 'AppImage' README.md` returns 0
- `grep -c 'install-on-steamdeck.sh' README.md` returns 0
- `test -f apps/frontend/src-tauri/ARCHITECTURE.md` returns 0
- `grep -c "in_flatpak" apps/frontend/src-tauri/ARCHITECTURE.md` is at least 2
- `grep -c "finish-args" flatpak/README.md` is at least 3
- `grep -c "added in a later phase" flatpak/README.md` returns 0
- `grep -c "Anti-Feature Checklist" flatpak/README.md` returns at least 1
</verification>

<success_criteria>
1. `README.md` install section uses Flatpak commands only — zero AppImage references
2. `apps/frontend/src-tauri/ARCHITECTURE.md` exists with 7+ sections covering build chain, Flatpak packaging, sandbox model, D-Bus gate, event pipeline, monorepo layout, and key decisions
3. `flatpak/README.md` documents all finish-args grouped by subsystem (display/BLE/gamepad), includes the anti-feature checklist with rejection reasons, and explains the `in_flatpak()` D-Bus gate with code reference
4. Cross-references: ARCHITECTURE.md links to flatpak/README.md and lib.rs; flatpak/README.md links to ARCHITECTURE.md
</success_criteria>

<output>
After completion, create `.planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-02-SUMMARY.md`
</output>
