---
phase: 13-sandbox-permissions-ble-gamepad
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src-tauri/src/lib.rs
  - flatpak/com.ks0555.robotcontroller.yaml
autonomous: true
requirements:
  - SBX-01
  - SBX-02
  - SBX-03
  - SBX-04
  - SBX-05
  - SBX-06
  - VAL-06
  - VAL-07

must_haves:
  truths:
    - "D-Bus address rewrite in lib.rs is skipped when running inside Flatpak"
    - "WEBKIT_DISABLE_COMPOSITING_MODE set_var in lib.rs runs unconditionally (inside and outside Flatpak)"
    - "Manifest finish-args include BLE system-bus talk names for org.bluez"
    - "Manifest finish-args include --device=input for evdev gamepad access"
    - "Manifest contains anti-feature review checklist as a comment block"
    - "Pre-existing display finish-args and WEBKIT env var remain intact"
  artifacts:
    - path: "apps/frontend/src-tauri/src/lib.rs"
      provides: "in_flatpak() helper + gated D-Bus block"
      contains: "fn in_flatpak"
    - path: "flatpak/com.ks0555.robotcontroller.yaml"
      provides: "BLE + gamepad finish-args + anti-feature checklist"
      contains: "--system-talk-name=org.bluez"
  key_links:
    - from: "lib.rs fn in_flatpak()"
      to: "D-Bus rewrite block"
      via: "if !in_flatpak() guard"
      pattern: "!in_flatpak\\(\\)"
    - from: "manifest finish-args"
      to: "Flatpak runtime D-Bus proxy"
      via: "--system-talk-name=org.bluez"
      pattern: "--system-talk-name=org\\.bluez"
    - from: "manifest finish-args"
      to: "/dev/input/event*"
      via: "--device=input"
      pattern: "--device=input"
---

<objective>
Gate lib.rs D-Bus address rewrite on `!in_flatpak` so btleplug uses the Flatpak runtime's system-bus proxy instead of an overwritten (broken) socket path. Add BLE and gamepad finish-args to the Flatpak manifest. Add anti-feature review checklist comment. Verify pre-existing display finish-args and WEBKIT env var.

Purpose: Fix Pitfall 13 (lib.rs D-Bus rewrite silently breaks BLE inside Flatpak) and Pitfall 2 (missing `--system-talk-name=org.bluez` causes silent BLE failure). Complete the sandbox permission layer for the v2.1 Flatpak milestone.
Output: Modified lib.rs with in_flatpak() guard, modified manifest with BLE + gamepad permissions + anti-feature checklist
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-sandbox-permissions-ble-gamepad/13-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md (Pitfall 2, Pitfall 13)
@apps/frontend/src-tauri/src/lib.rs
@flatpak/com.ks0555.robotcontroller.yaml

<interfaces>
<!-- Key code the executor needs. Extracted from codebase. -->

From apps/frontend/src-tauri/src/lib.rs (current state):
```rust
// Lines 12-20 — D-Bus rewrite block (MUST be gated on !in_flatpak per D-02):
if std::env::var("DBUS_SYSTEM_BUS_ADDRESS").is_err() {
    let steamos_socket = "/run/host/run/dbus/system_bus_socket";
    if std::path::Path::new(steamos_socket).exists() {
        std::env::set_var(
            "DBUS_SYSTEM_BUS_ADDRESS",
            format!("unix:path={}", steamos_socket),
        );
    }
}

// Line 24 — WEBKIT set_var (MUST remain ungated per D-03):
std::env::set_var("WEBKIT_DISABLE_COMPOSITING_MODE", "1");
```

From flatpak/com.ks0555.robotcontroller.yaml (current finish-args):
```yaml
finish-args:
  - --socket=wayland          # SBX-03 ✓ pre-existing
  - --socket=fallback-x11     # SBX-03 ✓ pre-existing
  - --share=ipc               # SBX-03 ✓ pre-existing
  - --device=dri              # SBX-03 ✓ pre-existing
  - --env=WEBKIT_DISABLE_COMPOSITING_MODE=1  # SBX-04 ✓ pre-existing
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add in_flatpak() helper and gate D-Bus rewrite in lib.rs</name>
  <files>apps/frontend/src-tauri/src/lib.rs</files>

  <read_first>
  Read before starting:
  - apps/frontend/src-tauri/src/lib.rs (full file, 47 lines) — understand existing D-Bus block structure
  - .planning/phases/13-sandbox-permissions-ble-gamepad/13-CONTEXT.md § Implementation Decisions (D-01, D-02, D-03)
  - .planning/research/PITFALLS.md § Pitfall 13 — understand why the rewrite breaks under Flatpak
  </read_first>

  <behavior>
    - Test 1: in_flatpak() returns true when FLATPAK_ID env var is set (run inside Flatpak sandbox)
    - Test 2: in_flatpak() returns true when /.flatpak-info file exists (belt-and-suspenders fallback)
    - Test 3: in_flatpak() returns false when neither signal is present (native cargo tauri dev)
    - Test 4: D-Bus rewrite block is wrapped in `if !in_flatpak() { ... }` — the entire if-is_err + path probe + set_var triplet
    - Test 5: WEBKIT_DISABLE_COMPOSITING_MODE set_var is NOT inside any !in_flatpak() guard (stays unconditional per D-03)
    - Test 6: cargo check passes in apps/frontend/src-tauri/
  </behavior>

  <action>
  Implement per user decisions D-01, D-02, D-03 from 13-CONTEXT.md:

  1. **Insert `fn in_flatpak() -> bool` before `pub fn run()`:**
     ```rust
     fn in_flatpak() -> bool {
         // Belt-and-suspenders Flatpak detection (D-01).
         // FLATPAK_ID is set by the Flatpak runtime at container start.
         // /.flatpak-info is the canonical file-based signal.
         std::env::var("FLATPAK_ID").is_ok()
             || std::path::Path::new("/.flatpak-info").exists()
     }
     ```
     Place it immediately after the `use` statements (after line 6) and before `pub fn run()`.

  2. **Gate the entire D-Bus rewrite block (lines 12-20) behind `!in_flatpak()`:**
     Wrap the existing `if std::env::var("DBUS_SYSTEM_BUS_ADDRESS").is_err() { ... }` block in:
     ```rust
     if !in_flatpak() {
         // Inside Flatpak the runtime proxies the system bus via DBUS_SYSTEM_BUS_ADDRESS.
         // Overwriting it with /run/host/run/dbus/ (which doesn't exist in the sandbox)
         // would break btleplug → BlueZ communication (Pitfall 13).
         // Only rewrite for native AppImage / host-side SteamOS where the socket path differs.
         if std::env::var("DBUS_SYSTEM_BUS_ADDRESS").is_err() {
             // ... existing code unchanged ...
         }
     }
     ```
     Add a `log::debug!` call inside the `!in_flatpak()` branch (before the inner if): `log::debug!("D-Bus rewrite: not in Flatpak, checking DBUS_SYSTEM_BUS_ADDRESS");` — this helps with Pitfall 13 diagnostics. Use `log` crate (already a transitive dependency via tauri).

  3. **LEAVE WEBKIT_DISABLE_COMPOSITING_MODE set_var (line 24) completely untouched.** Per D-03: inside Flatpak it's a harmless no-op (manifest already sets same value), outside Flatpak it's still needed.

  **IMPORTANT:** Do NOT modify any other code in lib.rs. Do NOT touch the `tauri::Builder` setup block, module declarations, or command handlers.
  </action>

  <verify>
    <automated>
      # Verify compilation
      cargo check 2>&1
      # Verify in_flatpak function exists
      grep -n "fn in_flatpak" apps/frontend/src-tauri/src/lib.rs
      # Verify D-Bus block is gated (check for !in_flatpak wrapping)
      grep -n "!in_flatpak" apps/frontend/src-tauri/src/lib.rs
      # Verify WEBKIT set_var is NOT inside any !in_flatpak guard
      # (It should appear after the closing brace of the outer !in_flatpak block)
      grep -n "WEBKIT_DISABLE_COMPOSITING_MODE" apps/frontend/src-tauri/src/lib.rs
      # Verify the D-Bus rewrite comment explains why it's skipped inside Flatpak
      grep -c "Pitfall 13" apps/frontend/src-tauri/src/lib.rs
    </automated>
  </verify>

  <acceptance_criteria>
    - `fn in_flatpak() -> bool` exists in lib.rs using belt-and-suspenders detection (FLATPAK_ID env OR /.flatpak-info file) per D-01
    - Entire D-Bus block (env var check + socket probe + set_var) is wrapped in `if !in_flatpak() { ... }` per D-02
    - Comment inside the gate explains that Flatpak runtime proxies system bus and overwriting would break BLE (mentions Pitfall 13)
    - WEBKIT_DISABLE_COMPOSITING_MODE set_var is NOT inside any Flatpak gate — runs unconditionally per D-03
    - `cargo check` passes in apps/frontend/src-tauri/
    - No other code in lib.rs was modified (module declarations, Tauri builder, command handlers unchanged)
  </acceptance_criteria>

  <done>
  lib.rs compiles, in_flatpak() helper guards D-Bus rewrite, WEBKIT set_var remains ungated. Pitfall 13 resolved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add BLE + gamepad finish-args and anti-feature checklist to Flatpak manifest</name>
  <files>flatpak/com.ks0555.robotcontroller.yaml</files>

  <read_first>
  Read before starting:
  - flatpak/com.ks0555.robotcontroller.yaml (full file, 38 lines) — understand current finish-args structure
  - .planning/phases/13-sandbox-permissions-ble-gamepad/13-CONTEXT.md § Implementation Decisions (D-05), § Specific Ideas (manifest additions + anti-feature checklist)
  - .planning/REQUIREMENTS.md § SBX-01, SBX-02, SBX-03, SBX-04, SBX-06
  - .planning/research/PITFALLS.md § Pitfall 2, Pitfall 3 — understand why --system-talk-name is needed, why --device=bluetooth is wrong
  </read_first>

  <action>
  Implement per user decisions D-05 and requirements SBX-01, SBX-02, SBX-06 from 13-CONTEXT.md:

  1. **Insert anti-feature review checklist comment at the top of the manifest** (before `id:`):
     ```yaml
     # === Anti-feature checklist (SBX-06) ===
     # Verify NONE of these are in finish-args:
     #   --filesystem=home          — Unnecessary sandbox escape
     #   --device=bluetooth         — Wrong stack (AF_BLUETOOTH not D-Bus; btleplug talks BlueZ over D-Bus)
     #   --talk-name=org.bluez      — Wrong bus (session, not system; need --system-talk-name)
     #   --socket=session-bus       — Tray icon arg, not needed for this app
     #   --socket=system-bus        — Over-broad; use --system-talk-name=org.bluez instead (principle of least privilege)
     #   org.freedesktop.Flatpak    — Portal grant, not needed (no portal interaction)
     ```
     Place this as the very first lines of the file, with a blank line between it and `id:`.

  2. **Add BLE finish-args** (SBX-01) to the `finish-args` list, appended after the existing `--env=WEBKIT_DISABLE_COMPOSITING_MODE=1` line:
     ```yaml
       # BLE — btleplug talks to BlueZ over the system D-Bus (Pitfall 2)
       - --system-talk-name=org.bluez
       - --system-talk-name=org.bluez.*
       - --allow=bluetooth
       - --share=network
     ```
     Order: group these together with the BLE comment line above them. The `--share=network` is needed because `--allow=bluetooth` requires it per Flatpak docs (AF_BLUETOOTH socket permission needs network access).

  3. **Add gamepad finish-args** (SBX-02) after the BLE group:
     ```yaml
       # Gamepad — gilrs reads from /dev/input/event* (Pitfall 4)
       - --device=input
       # --device=all  # fallback if --device=input not available (Flatpak < 1.15.6 on older SteamOS)
     ```
     The `--device=input` comment line should remain as a YAML comment (starting with `#`).

  4. **Verify pre-existing finish-args are intact** (SBX-03, SBX-04):
     - `--socket=wayland` — present ✓
     - `--socket=fallback-x11` — present ✓
     - `--share=ipc` — present ✓
     - `--device=dri` — present ✓
     - `--env=WEBKIT_DISABLE_COMPOSITING_MODE=1` — present ✓
     These were added in Phase 12. Do NOT duplicate or remove them.

  **IMPORTANT:**
  - Do NOT modify the `modules:` section, `build-commands`, or `sources:`.
  - Do NOT add `--device=bluetooth` — that's for raw AF_BLUETOOTH sockets, which btleplug does not use (Pitfall 3). btleplug uses D-Bus only.
  - Do NOT add `--talk-name=org.bluez` (session bus) — BlueZ is on the system bus. Use `--system-talk-name`.
  - Do NOT add `--filesystem=home` or any filesystem grants — not needed.
  - The comment starting `# --device=all` is intentionally left as a YAML comment (not an active finish-arg) — it documents the fallback path for older SteamOS Flatpak versions.

  The final `finish-args` list should read (in order):
  ```yaml
  finish-args:
    - --socket=wayland
    - --socket=fallback-x11
    - --share=ipc
    - --device=dri
    - --env=WEBKIT_DISABLE_COMPOSITING_MODE=1
    # BLE — btleplug talks to BlueZ over the system D-Bus (Pitfall 2)
    - --system-talk-name=org.bluez
    - --system-talk-name=org.bluez.*
    - --allow=bluetooth
    - --share=network
    # Gamepad — gilrs reads from /dev/input/event* (Pitfall 4)
    - --device=input
    # --device=all  # fallback if --device=input not available (Flatpak < 1.15.6 on older SteamOS)
  ```
  </action>

  <verify>
    <automated>
      # Verify anti-feature checklist comment exists at top of manifest
      head -15 flatpak/com.ks0555.robotcontroller.yaml | grep -c "Anti-feature checklist"
      # Verify BLE finish-args are present
      grep -c "\-\-system-talk-name=org\.bluez" flatpak/com.ks0555.robotcontroller.yaml
      grep -c "\-\-allow=bluetooth" flatpak/com.ks0555.robotcontroller.yaml
      grep -c "\-\-share=network" flatpak/com.ks0555.robotcontroller.yaml
      # Verify gamepad finish-args are present
      grep -c "\-\-device=input" flatpak/com.ks0555.robotcontroller.yaml
      grep -c "\-\-device=all.*fallback" flatpak/com.ks0555.robotcontroller.yaml
      # Verify pre-existing display finish-args are still present (SBX-03)
      grep -c "\-\-socket=wayland" flatpak/com.ks0555.robotcontroller.yaml
      grep -c "\-\-socket=fallback-x11" flatpak/com.ks0555.robotcontroller.yaml
      grep -c "\-\-share=ipc" flatpak/com.ks0555.robotcontroller.yaml
      grep -c "\-\-device=dri" flatpak/com.ks0555.robotcontroller.yaml
      # Verify WEBKIT_DISABLE_COMPOSITING_MODE env var still present (SBX-04)
      grep -c "WEBKIT_DISABLE_COMPOSITING_MODE" flatpak/com.ks0555.robotcontroller.yaml
      # Verify anti-features are NOT present (SBX-06)
      grep -c "\-\-filesystem=home" flatpak/com.ks0555.robotcontroller.yaml; test $? -ne 0
      grep -c "\-\-device=bluetooth" flatpak/com.ks0555.robotcontroller.yaml; test $? -ne 0
      grep "\-\-talk-name=org\.bluez" flatpak/com.ks0555.robotcontroller.yaml | grep -v "system-talk-name"; test $? -ne 0
      # Verify modules section is unchanged (structure check)
      grep -c "modules:" flatpak/com.ks0555.robotcontroller.yaml
      # Verify YAML is valid (parse with python)
      python3 -c "import yaml; yaml.safe_load(open('flatpak/com.ks0555.robotcontroller.yaml'))" 2>&1
    </automated>
  </verify>

  <acceptance_criteria>
    - Anti-feature checklist comment block exists at the top of the manifest (before `id:`) listing 6 forbidden finish-args with explanations per D-05
    - `--system-talk-name=org.bluez` and `--system-talk-name=org.bluez.*` present in finish-args (SBX-01)
    - `--allow=bluetooth` and `--share=network` present in finish-args (SBX-01)
    - `--device=input` present with `# --device=all` documented as commented fallback (SBX-02)
    - Pre-existing display finish-args (`--socket=wayland`, `--socket=fallback-x11`, `--share=ipc`, `--device=dri`) intact (SBX-03)
    - Pre-existing `--env=WEBKIT_DISABLE_COMPOSITING_MODE=1` intact (SBX-04)
    - NO anti-features present: no `--filesystem=home`, no `--device=bluetooth`, no bare `--talk-name=org.bluez`, no `--socket=session-bus`, no `--socket=system-bus`, no `org.freedesktop.Flatpak` (SBX-06)
    - YAML parses successfully
    - `modules:`, `build-commands:`, and `sources:` sections are unchanged
  </acceptance_criteria>

  <done>
  Manifest contains BLE + gamepad finish-args, anti-feature checklist comment, pre-existing display/WEBKIT args intact. Pitfall 2 and Pitfall 3 resolved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host system ↔ Flatpak sandbox | finish-args define what crosses this boundary; the sandbox restricts filesystem, device, and D-Bus access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure / Elevation of Privilege | `--device=all` (if uncommented) | mitigate | Use `--device=input` (minimum permission). `--device=all` is kept as a YAML comment only — documented fallback for Flatpak < 1.15.6. Anti-feature checklist (SBX-06) warns against blanket device access. |
| T-13-02 | Tampering | lib.rs D-Bus address rewrite | mitigate | Gate entire D-Bus block behind `!in_flatpak()`. Inside Flatpak the runtime's proxy sets `DBUS_SYSTEM_BUS_ADDRESS` correctly — the old code would overwrite it with a non-existent `/run/host/run/dbus/` path, silently breaking BLE (Pitfall 13). |
| T-13-03 | Elevation of Privilege | `--filesystem=home` or `--talk-name=org.freedesktop.Flatpak` in finish-args | mitigate | Anti-feature checklist (SBX-06) at top of manifest documents these as forbidden. Neither is present in the active finish-args list. |
| T-13-04 | Information Disclosure | `--device=bluetooth` (wrong stack) in finish-args | mitigate | Anti-feature checklist documents `--device=bluetooth` as wrong approach. btleplug uses D-Bus, not AF_BLUETOOTH sockets. Only `--system-talk-name=org.bluez` is present. |
</threat_model>

<verification>
## Automated Verification (Nyquist)

Both tasks include grep-based automated verification commands. Run after each task:

```bash
# Task 1 verification
cargo check --manifest-path apps/frontend/src-tauri/Cargo.toml 2>&1
grep -n "fn in_flatpak" apps/frontend/src-tauri/src/lib.rs
grep -n "!in_flatpak" apps/frontend/src-tauri/src/lib.rs

# Task 2 verification
python3 -c "import yaml; yaml.safe_load(open('flatpak/com.ks0555.robotcontroller.yaml'))"
grep -c "\-\-system-talk-name=org\.bluez" flatpak/com.ks0555.robotcontroller.yaml
grep -c "\-\-device=input" flatpak/com.ks0555.robotcontroller.yaml
head -15 flatpak/com.ks0555.robotcontroller.yaml | grep -c "Anti-feature checklist"
```

## Manual Verification (Hardware Required)

Per D-04, VAL-06 and VAL-07 require real hardware (BT24 robot + gamepad). These cannot be automated. After plan execution:

**VAL-06 (BLE through sandbox):**
1. Build Flatpak: `cd flatpak && ./build.sh`
2. Install: `flatpak install --user --reinstall robot-controller.flatpak`
3. Power on BT24 robot (blue LED blinking = advertising)
4. Launch app: `flatpak run com.ks0555.robotcontroller`
5. Click "Connect" — verify `ble-state-changed=connected` event
6. Verify BLE D-Bus proxy: `dbus-monitor --system` from host shows btleplug talking to `org.bluez`
7. Verify: `flatpak run --command=sh com.ks0555.robotcontroller -c "busctl --system list | grep bluez"` shows `org.bluez` visible
8. Click "Disconnect" — verify `ble-state-changed=disconnected`

**VAL-07 (gamepad through sandbox):**
1. Connect a gamepad (or use built-in Steam Deck controller)
2. Launch: `flatpak run com.ks0555.robotcontroller`
3. Verify: `flatpak run --command=ls com.ks0555.robotcontroller /dev/input` lists `event*` nodes
4. Move joystick — verify `gamepad-direction` events fire (check frontend UI updates direction)
5. Press gamepad buttons mapped to commands — verify robot responds (F/B/L/R/S)

**Failure diagnostics:**
- If BLE fails: `RUST_LOG=btleplug=debug flatpak run com.ks0555.robotcontroller` — look for `dbus error: NameHasNoOwner`
- If gamepad fails: `flatpak run --command=ls com.ks0555.robotcontroller /dev/input` — empty output means `--device=input` not working
</verification>

<success_criteria>
1. `cargo check` passes in `apps/frontend/src-tauri/` — lib.rs compiles with `in_flatpak()` helper
2. D-Bus rewrite block is gated behind `!in_flatpak()` with explanatory comment mentioning Pitfall 13
3. WEBKIT_DISABLE_COMPOSITING_MODE set_var remains unconditional (not inside any Flatpak guard)
4. Manifest contains `--system-talk-name=org.bluez`, `--system-talk-name=org.bluez.*`, `--allow=bluetooth`, `--share=network`
5. Manifest contains `--device=input` with commented `# --device=all` fallback
6. Manifest anti-feature checklist comment block lists all 6 forbidden finish-args
7. Pre-existing display finish-args and WEBKIT env var are intact
8. No anti-features present in active finish-args
9. YAML parses without errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-sandbox-permissions-ble-gamepad/13-01-SUMMARY.md`
</output>
