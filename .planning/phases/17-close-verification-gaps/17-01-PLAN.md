---
phase: 17-close-verification-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-sandbox-permissions-ble-gamepad/13-VERIFICATION.md
  - .planning/phases/15-ci-migration-parallel-run/15-VERIFICATION.md
  - .planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-VERIFICATION.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Phase 13 success criteria are independently verifiable with concrete commands"
    - "Phase 15 success criteria are independently verifiable with concrete commands"
    - "Phase 16 success criteria are independently verifiable with concrete commands"
    - "Each VERIFICATION.md has a success-criterion table with verification method, expected result, and evidence column"
    - "Every verification method references specific files, commands, grep patterns, or CLI invocations"
  artifacts:
    - path: ".planning/phases/13-sandbox-permissions-ble-gamepad/13-VERIFICATION.md"
      provides: "Verification criteria for Phase 13 (sandbox permissions)"
      min_lines: 30
      contains: "Phase 13"
    - path: ".planning/phases/15-ci-migration-parallel-run/15-VERIFICATION.md"
      provides: "Verification criteria for Phase 15 (CI migration)"
      min_lines: 30
      contains: "Phase 15"
    - path: ".planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-VERIFICATION.md"
      provides: "Verification criteria for Phase 16 (AppImage decommission)"
      min_lines: 30
      contains: "Phase 16"
  key_links:
    - from: "13-VERIFICATION.md"
      to: "13-CONTEXT.md"
      via: "Decision reference"
      pattern: "D-0[1-5]"
    - from: "15-VERIFICATION.md"
      to: "15-CONTEXT.md"
      via: "Decision reference"
      pattern: "D-0[1-9]|D-1[0-9]|D-2[0-9]"
    - from: "16-VERIFICATION.md"
      to: "16-CONTEXT.md"
      via: "Decision reference"
      pattern: "D-0[1-9]|D-1[0-9]|D-2[0-9]"
---

<objective>
Create VERIFICATION.md documents for Phases 13, 15, and 16 that make every success criterion independently verifiable with concrete evidence (specific commands, log patterns, file checks).

**Purpose:** Close the verification gap identified in ROADMAP Phase 17. Each completed phase currently lacks a formal verification document — this plan creates one per phase that any developer can use to confirm the phase was implemented correctly.

**Output:** 3 VERIFICATION.md files in each target phase's directory.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<template>
<VERIFICATION_TEMPLATE>
# Phase {N}: {Name} — Verification

**Created:** {date}
**Phase status:** Complete
**Phase goal:** {goal}

## Success Criteria Verification

{For each success criterion from ROADMAP.md for this phase:}

### SC-{N}: {Criterion description}

| Aspect | Detail |
|--------|--------|
| **Source** | ROADMAP.md Phase {N} Success Criterion {N} |
| **What to verify** | {description} |
| **Verification method** | {concrete command / file check / log pattern / grep} |
| **Expected result** | {exact output or state} |
| **Evidence** | {path to evidence or how to capture} |
| **Automated** | {yes/no/partial — why} |

{Under each SC, list each D-XX decision from CONTEXT.md that this criterion touches:}

### D-XX: {Decision description}

| Aspect | Detail |
|--------|--------|
| **Source** | {phase}-CONTEXT.md |
| **What to verify** | {decision in plain language} |
| **Verification method** | {concrete grep / file read / CLI command} |
| **Expected result** | {exact state} |
| **Evidence** | {file + line or command output} |

## Overall Verification Summary

| Criterion | Status | Evidence |
|-----------|--------|----------|
| SC-1 | | |
| SC-2 | | |
| SC-3 | | |
| SC-4 | | |
| SC-5 | | |

Each row left blank — to be filled during execution verification.
</VERIFICATION_TEMPLATE>
</template>

<context>
@.planning/ROADMAP.md (Phase 13, 15, 16 sections)
@.planning/phases/13-sandbox-permissions-ble-gamepad/13-CONTEXT.md
@.planning/phases/13-sandbox-permissions-ble-gamepad/13-01-SUMMARY.md
@.planning/phases/15-ci-migration-parallel-run/15-CONTEXT.md
@.planning/phases/15-ci-migration-parallel-run/15-01-SUMMARY.md
@.planning/phases/15-ci-migration-parallel-run/15-02-SUMMARY.md
@.planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-CONTEXT.md
@.planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-01-SUMMARY.md
@.planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-02-SUMMARY.md
@.planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VERIFICATION.md for Phase 13 (Sandbox Permissions)</name>
  <files>.planning/phases/13-sandbox-permissions-ble-gamepad/13-VERIFICATION.md</files>
  <read_first>.planning/phases/13-sandbox-permissions-ble-gamepad/13-CONTEXT.md</read_first>
  <action>
    Create .planning/phases/13-sandbox-permissions-ble-gamepad/13-VERIFICATION.md using the VERIFICATION_TEMPLATE.

    Extract from ROADMAP.md the 5 success criteria for Phase 13:

    SC-1: "Locally, `flatpak run com.ks0555.robotcontroller` connects to a real BT24 robot via BLE; `dbus-monitor --system` from a host shell shows btleplug talking to `org.bluez` through the Flatpak D-Bus proxy; `busctl --system list` from inside `flatpak run --command=sh` lists `org.bluez`"
      → Verification method: two-command sequence. Host: `dbus-monitor --system 'type=signal,interface=org.freedesktop.DBus' timeout 10` while connecting. Inside sandbox: `flatpak run --command=sh com.ks0555.robotcontroller -c 'busctl --system list | grep bluez'`

    SC-2: "Locally, `flatpak run` reads gamepad input via gilrs; `flatpak run --command=ls com.ks0555.robotcontroller /dev/input` lists `event*` nodes; the Steam-named controller emits `gamepad-direction` events"
      → Verification method: `flatpak run --command=ls com.ks0555.robotcontroller /dev/input 2>&1 | grep -c event` must return >= 1. Event emission verified via `flatpak run com.ks0555.robotcontroller` with `RUST_LOG=debug` and checking for "gamepad-direction" in stderr, or by frontend observation.

    SC-3: "Manifest `finish-args` includes BLE flags (`--system-talk-name=org.bluez`, `--system-talk-name=org.bluez.*`, `--allow=bluetooth`, `--share=network`), gamepad flags (`--device=input` with `--device=all` documented as fallback comment), display flags (`--socket=wayland`, `--socket=fallback-x11`, `--share=ipc`, `--device=dri`), and `--env=WEBKIT_DISABLE_COMPOSITING_MODE=1`"
      → Verification method: For each flag, `grep -c` in flatpak/com.ks0555.robotcontroller.yaml.
      Must find each flag at least once. Ensure `--device=all` appears only in a comment (prefixed by `#`). Count match lines; if a flag appears twice (code + comment) that's fine, as long as each required flag is present at least once.

    SC-4: "`apps/frontend/src-tauri/src/lib.rs` `DBUS_SYSTEM_BUS_ADDRESS=/run/host/run/dbus/system_bus_socket` rewrite is gated behind a Flatpak detection check (`/.flatpak-info` exists or `FLATPAK_ID` env var set); inside Flatpak the rewrite is a no-op and a comment explains why"
      → Verification method: `grep -n 'in_flatpak' apps/frontend/src-tauri/src/lib.rs` must show the function definition. `grep -n 'if !in_flatpak' apps/frontend/src-tauri/src/lib.rs` must show the gate wrapping the D-Bus block. `grep -n 'FLATPAK_ID\|/.flatpak-info' apps/frontend/src-tauri/src/lib.rs` must show both detection methods.

    SC-5: "Manifest contains NO anti-features: no `--filesystem=home`, no `--device=bluetooth` (wrong stack — AF_BLUETOOTH not D-Bus), no `--talk-name=org.bluez` (wrong bus — session not system), no tray-icon args, no `org.freedesktop.Flatpak` portal grant — verified by manual review checklist comment in the manifest"
      → Verification method: `grep -c '\-\-filesystem=home' flatpak/com.ks0555.robotcontroller.yaml` must return 0 outside comments. The anti-feature checklist comment must be present: `grep -c 'Anti-feature checklist\|SBX-06' flatpak/com.ks0555.robotcontroller.yaml` must return at least 1. Each forbidden arg must appear only in the checklist comment, not in the finish-args list.

    Also extract all D-XX decisions from 13-CONTEXT.md that are relevant to the success criteria. Map each decision to its verification:
    - D-01 (belt-and-suspenders FLATPAK_ID + /.flatpak-info): covered by SC-4 grep checks
    - D-02 (D-Bus block gated behind !in_flatpak): covered by SC-4
    - D-03 (WEBKIT set_var unconditional): verify via grep for `set_var.*WEBKIT_DISABLE_COMPOSITING_MODE` in lib.rs — must be present OUTSIDE the `if !in_flatpak()` block
    - D-04 (VAL-06/VAL-07 manual hardware): verify via SUMMARY.md reference to manual validation
    - D-05 (anti-feature checklist at top of manifest): covered by SC-5

    Write the complete VERIFICATION.md with concrete commands. Do NOT leave placeholders — every command must be a real shell command that can be run.
  </action>
  <acceptance_criteria>
    - 13-VERIFICATION.md exists at the phase directory
    - File contains exactly 5 success criterion tables (SC-1 through SC-5)
    - File contains a D-XX decision subsection for each relevant decision
    - All 4 BLE finish-args have verification commands listed
    - Both gamepad finish-args verification commands listed
    - Display finish-args verification commands listed
    - lib.rs in_flatpak() gate verification via 3 grep commands
    - Anti-feature checklist verification via grep
  </acceptance_criteria>
  <verify>
    <automated>
      test -f .planning/phases/13-sandbox-permissions-ble-gamepad/13-VERIFICATION.md \
        && grep -c "SC-1" .planning/phases/13-sandbox-permissions-ble-gamepad/13-VERIFICATION.md | grep -q 1 \
        && grep -c "SC-2" .planning/phases/13-sandbox-permissions-ble-gamepad/13-VERIFICATION.md | grep -q 1 \
        && grep -c "SC-3" .planning/phases/13-sandbox-permissions-ble-gamepad/13-VERIFICATION.md | grep -q 1 \
        && grep -c "SC-4" .planning/phases/13-sandbox-permissions-ble-gamepad/13-VERIFICATION.md | grep -q 1 \
        && grep -c "SC-5" .planning/phases/13-sandbox-permissions-ble-gamepad/13-VERIFICATION.md | grep -q 1 \
        && echo "All 5 SC tables present" || echo "MISSING CRITERIA"
    </automated>
  </verify>
  <done>13-VERIFICATION.md has all 5 success criteria with concrete verification commands. Every D-XX from 13-CONTEXT.md has a traceable verification entry. File is at least 30 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Create VERIFICATION.md for Phase 15 (CI Migration)</name>
  <files>.planning/phases/15-ci-migration-parallel-run/15-VERIFICATION.md</files>
  <read_first>.planning/phases/15-ci-migration-parallel-run/15-CONTEXT.md</read_first>
  <action>
    Create .planning/phases/15-ci-migration-parallel-run/15-VERIFICATION.md using the VERIFICATION_TEMPLATE.

    Extract from ROADMAP.md the 5 success criteria for Phase 15:

    SC-1: "`.github/workflows/build.yml` includes a `build-flatpak-x64` job using `flatpak/flatpak-github-actions/flatpak-builder@v6.7` with the Flathub container image matching PKG-04 (`ghcr.io/flathub-infra/flatpak-github-actions:freedesktop-24.08`)"
      → Verification method: grep for both `flatpak-builder@v6` and `freedesktop-24.08` in `.github/workflows/build.yml`. Also grep for `build-flatpak-x64` job header.

    SC-2: "Tagged release CI uploads `RobotController-x86_64.flatpak` as a release asset alongside the existing AppImage; at least one tagged release ships both artifacts (parallel-run window) before AppImage is removed in Phase 16"
      → Verification method: Check that `build-flatpak-x64` job has `softprops/action-gh-release@v2` step. Check that flatpak job uploads `RobotController-*` assets. Check that `build-x64` job still exists (parallel-run window open). Verify via `grep -c 'upload-artifact' .github/workflows/build.yml` — must be non-zero (deb artifact still uploaded).

    SC-3: "The `build-arm64` job is removed from `.github/workflows/build.yml` (Steam Deck is x86_64 only); the `build-macos` job was already removed in Phase 11 — Phase 15 does not re-add it"
      → Verification method: `grep -c 'arm64\|aarch64' .github/workflows/build.yml` must return 0 (outside comments). `grep -c 'macos\|macOS' .github/workflows/build.yml` must return 0 outside comments.

    SC-4: "OSTree cache is enabled (`cache: true`) on the flatpak-builder action — runtime download (~1 GB) is reused across runs; warm-cache CI run completes within an acceptable budget (documented in commit message)"
      → Verification method: grep for `cache: true` in the flatpak-builder action step. grep for a `cache-key` that includes the manifest hash and runtime version. Verify via `grep -A 10 'flatpak-builder' .github/workflows/build.yml | grep -c 'cache:.*true'`.

    SC-5: "`app.tsx`, `control-pad.tsx`, `status-bar.tsx` remain unchanged after the CI migration — the existing `git diff --exit-code` lock check in CI passes (covers VAL-08)"
      → Verification method: grep for `git diff --exit-code` in `.github/workflows/build.yml`. grep for each locked file name after the diff command. Verify the lock check is present and references `apps/frontend/src/app.tsx`.

    Also extract all D-XX decisions from 15-CONTEXT.md that map to success criteria. Per the SUMMARY, there are 29 D-XX decisions (D-01 through D-29). Group by relevance:

    D-01 through D-13 (Release Upload Strategy) → SC-2 verification
    - D-01: Only .flatpak is release asset → confirm no .deb release upload
    - D-02: softprops/action-gh-release@v2 → grep for the action
    - D-03: Upload only on tag push → grep for `startsWith.*refs/tags` or similar
    - D-04: Per-job contents:write → confirm per-job permissions pattern
    - D-05: Versioned filenames from ref_name → grep for version extraction
    - D-13: skip_release workflow_dispatch input → grep for skip_release in inputs

    D-14 through D-19 (Job Dependency Model) → SC-1 verification
    - D-14/D-15: Artifact passing via upload/download-artifact@v4 → grep for these actions
    - D-16: CI prep steps (download deb, copy to flatpak/) → grep for copy/mv in flatpak job
    - D-17: flatpak-github-actions with container → grep for the container ref
    - D-18: ubuntu-24.04 runner → grep for runner image
    - D-19: No arm64 → covered by SC-3

    D-20 through D-22 (Artifact Retention Policy) → SC-2 verification
    - D-20: .flatpak on every tagged release → confirm release step exists
    - D-22: .deb is workflow artifact only → confirm no release upload

    D-23 through D-25 (OSTree Cache Config) → SC-4 verification
    - D-23: cache: true on flatpak-builder → grep check
    - D-24: actions/cache for cargo-registry, cargo-target, pnpm-store → grep for each cache
    - D-25: OSTree cache key format → grep for cache-key pattern

    D-26 through D-29 (Locked Constraints) → SC-5 verification
    - D-27: VAL-08 git diff --exit-code → covered by SC-5

    Write the complete VERIFICATION.md. Every verification method must be a real shell command.
  </action>
  <acceptance_criteria>
    - 15-VERIFICATION.md exists at the phase directory
    - File contains exactly 5 success criterion tables (SC-1 through SC-5)
    - File references each major D-XX cluster with corresponding verification commands
    - flatpak-builder action verification includes container image reference
    - OSTree cache verification via `cache: true` + `cache-key` grep
    - Release asset verification via softprops action grep
    - No arm64/aarch64 references verification via grep
    - Locked files verification via git diff --exit-code grep
  </acceptance_criteria>
  <verify>
    <automated>
      test -f .planning/phases/15-ci-migration-parallel-run/15-VERIFICATION.md \
        && grep -c "SC-1" .planning/phases/15-ci-migration-parallel-run/15-VERIFICATION.md | grep -q 1 \
        && grep -c "SC-2" .planning/phases/15-ci-migration-parallel-run/15-VERIFICATION.md | grep -q 1 \
        && grep -c "SC-3" .planning/phases/15-ci-migration-parallel-run/15-VERIFICATION.md | grep -q 1 \
        && grep -c "SC-4" .planning/phases/15-ci-migration-parallel-run/15-VERIFICATION.md | grep -q 1 \
        && grep -c "SC-5" .planning/phases/15-ci-migration-parallel-run/15-VERIFICATION.md | grep -q 1 \
        && echo "All 5 SC tables present" || echo "MISSING CRITERIA"
    </automated>
  </verify>
  <done>15-VERIFICATION.md has all 5 success criteria with concrete CI-pipeline verification commands. All relevant D-XX clusters have traceable verification entries. File is at least 30 lines.</done>
</task>

<task type="auto">
  <name>Task 3: Create VERIFICATION.md for Phase 16 (AppImage Decommission)</name>
  <files>.planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-VERIFICATION.md</files>
  <read_first>.planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-CONTEXT.md</read_first>
  <action>
    Create .planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-VERIFICATION.md using the VERIFICATION_TEMPLATE.

    Extract from ROADMAP.md the 5 success criteria for Phase 16:

    SC-1: "AppImage `build-x64` job and AppImage release asset are removed from `.github/workflows/build.yml` in a separate PR landed only after Phase 15 has shipped at least one tagged release with both artifacts; the parallel-run window is closed"
      → Verification method: grep for `build-x64` in `.github/workflows/build.yml` — must return 0. grep for `appimage` (case-insensitive) in build.yml — must return 0 outside comments. The phase is complete so the parallel-run window is confirmed closed.

    SC-2: "Root README install section walks a Steam Deck user through `flatpak install --user RobotController-x86_64.flatpak`, 'Add as Non-Steam Game' steps, and Gaming Mode launch (text walkthrough; screenshots optional)"
      → Verification method: grep for `flatpak install --user` in README.md — must return >= 1. grep for `Add as Non-Steam Game\|Non-Steam` in README.md — must return >= 1. grep for `Gaming Mode\|gaming mode\|Gaming mode` in README.md — must return >= 1. grep for `AppImage\|appimage` in README.md — must return 0 (no stale references).

    SC-3: "Documentation accurately describes the upgrade path: `flatpak install --user --reinstall RobotController-x86_64.flatpak` (sideload bundles cannot use `flatpak update`); an optional GitHub Releases polling launcher script is provided or explicitly marked deferred"
      → Verification method: grep for `flatpak install --user --reinstall\|flatpak.*--reinstall` in README.md — must return >= 1. Also check `upgrade-robot-controller.sh` exists at repo root — `test -f upgrade-robot-controller.sh`. Check the script has `--check` and `--force` flags: `grep -c '\-\-check' upgrade-robot-controller.sh` >= 1 and `grep -c '\-\-force' upgrade-robot-controller.sh` >= 1.

    SC-4: "`apps/frontend/src-tauri/README.md` (or root) documents the deb-extract Flatpak architecture and the `lib.rs` `!in_flatpak` D-Bus gate; `flatpak/README.md` contributor guide reproduces the sandbox finish-args rationale; `justfile` adds `flatpak-build`, `flatpak-install`, `flatpak-run`, `flatpak-deploy` recipes (DOCS-04 optional but useful)"
      → Verification method: Check `apps/frontend/src-tauri/ARCHITECTURE.md` exists. grep for `deb-extract\|deb extract` in the file — must return >= 1. grep for `in_flatpak` in the file — must return >= 1. Check `flatpak/README.md` has finish-args rationale: `grep -c 'finish-args\|finish.args' flatpak/README.md` >= 3. Check justfile: `just justfile 2>&1 | grep -c 'flatpak-build'` >= 1 or simply grep for `flatpak-build` in justfile.

    SC-5: "Final CI run on `main` confirms `app.tsx`, `control-pad.tsx`, `status-bar.tsx` unchanged across the entire v2.1 milestone (`git diff --exit-code` lock check holds end-to-end — VAL-08 satisfied)"
      → Verification method: Run `git diff HEAD -- apps/frontend/src/app.tsx apps/frontend/src/components/control-pad.tsx apps/frontend/src/components/status-bar.tsx` — must return no output. If pre-commit hooks enforce this, the lock is implicit. Also verify via `git log --oneline --all -- apps/frontend/src/app.tsx | head -5` shows no commits touching these files since Phase 10.

    Also verify all D-XX decisions from 16-CONTEXT.md that map to success criteria. The known decisions from the three plans:

    D-01 through D-15 (CI Consolidation per 16-01-SUMMARY) → SC-1
    - D-01: Remove build-x64, rename to build → verify no `build-x64` job
    - D-02: VAL-08 dropped from CI → verify no `git diff --exit-code` in build.yml
    - D-04: Version from Cargo.toml → verify `cargo metadata` or `Cargo.toml` ref
    - D-05: Remove cancel-in-progress → verify no `cancel-in-progress` in build.yml
    - D-06: Deb built inline, no artifact upload → verify no upload-artifact for deb
    - D-08: Delete install-on-steamdeck.sh → verify file does not exist
    - D-09: Full AppImage cleanup → verify no AppImage refs in build.yml/RUNNING.md
    - D-10: pnpm-store cache → verify actions/cache for pnpm-store
    - D-11: Job-level contents:write → verify per-job permissions

    D-20 through D-22 (Documentation per 16-02-SUMMARY) → SC-2, SC-3
    - D-20: ARCHITECTURE.md → covered by SC-4
    - D-21: flatpak/README.md updates → covered by SC-4
    - D-22: Root README rewrite → covered by SC-2

    D-16 through D-19 (Polling Launcher per 16-03-SUMMARY) → SC-3
    - D-16: upgrade-robot-controller.sh at repo root → verify file exists
    - D-17: Dual-purpose install/upgrade → verify both paths in the script
    - D-18: Full upgrade assistant UX → verify --check/--force/--version flags

    D-23 through D-27 (justfile per 16-03-SUMMARY) → SC-4
    - D-24: flatpak-build recipe → verify in justfile
    - D-25: flatpak-install → verify
    - D-26: flatpak-run → verify
    - D-27: flatpak-deploy scp only → verify

    Write the complete VERIFICATION.md with real shell commands.
  </action>
  <acceptance_criteria>
    - 16-VERIFICATION.md exists at the phase directory
    - File contains exactly 5 success criterion tables (SC-1 through SC-5)
    - SC-1 verifies build-x64 removal, AppImage reference cleanup
    - SC-2 verifies README.md has flatpak install, Non-Steam Game, Gaming Mode sections
    - SC-2 verifies README.md has NO AppImage references
    - SC-3 verifies upgrade-robot-controller.sh existence and flags
    - SC-4 verifies ARCHITECTURE.md, flatpak/README.md, and justfile recipes
    - SC-5 verifies app.tsx unchanged via git diff
    - Each D-XX cluster has at least one verification entry
  </acceptance_criteria>
  <verify>
    <automated>
      test -f .planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-VERIFICATION.md \
        && grep -c "SC-1" .planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-VERIFICATION.md | grep -q 1 \
        && grep -c "SC-2" .planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-VERIFICATION.md | grep -q 1 \
        && grep -c "SC-3" .planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-VERIFICATION.md | grep -q 1 \
        && grep -c "SC-4" .planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-VERIFICATION.md | grep -q 1 \
        && grep -c "SC-5" .planning/phases/16-appimage-decommission-upgrade-workflow-docs/16-VERIFICATION.md | grep -q 1 \
        && echo "All 5 SC tables present" || echo "MISSING CRITERIA"
    </automated>
  </verify>
  <done>16-VERIFICATION.md has all 5 success criteria with concrete verification commands. All D-XX from plans 01-03 have traceable entries. File is at least 30 lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this phase creates only verification documentation (VERIFICATION.md files). No application code, CI configuration, or infrastructure is created or modified.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | (none) | VERIFICATION.md | accept | Documentation only — no code, no attack surface |
</threat_model>

<verification>
1. All 3 VERIFICATION.md files exist in their respective phase directories
2. Each file contains exactly 5 success criterion tables matching ROADMAP.md
3. Each file references the relevant D-XX decisions from CONTEXT.md
4. Every verification method uses concrete commands, not hand-wavy descriptions
5. Each file is at least 30 lines
</verification>

<success_criteria>
- Phase 13 verification: All 5 SC tables present with D-Bus, gamepad, finish-args, lib.rs gate, and anti-feature checklist verification
- Phase 15 verification: All 5 SC tables present with flatpak CI job, release upload, arm64 removal, OSTree cache, and VAL-08 lock check verification
- Phase 16 verification: All 5 SC tables present with AppImage cleanup, README accuracy, upgrade script, architecture docs, and app.tsx unchanged verification
</success_criteria>

<output>
After completion, create .planning/phases/17-close-verification-gaps/17-01-SUMMARY.md
</output>
