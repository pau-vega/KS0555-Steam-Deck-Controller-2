---
phase: 05-eslint-config-typescript-conversion
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - apps/backend/package.json
  - apps/frontend/package.json
  - packages/eslint-config/package.json
autonomous: false
requirements: [CLEAN-04]
user_setup: []
must_haves:
  truths:
    - "Both apps can import eslint-config without errors after conversion"
    - "pnpm build passes with zero errors"
    - "pnpm typecheck passes with zero errors"
    - "pnpm lint passes with zero errors"
  artifacts:
    - path: "apps/backend/package.json"
      provides: "Updated lint script pointing to node.ts or dist"
      contains: "eslint-config"
    - path: "apps/frontend/package.json"
      provides: "Updated lint script pointing to react.ts or dist"
      contains: "eslint-config"
  key_links:
    - from: "apps/backend/package.json"
      to: "packages/eslint-config/src/node.ts"
      via: "lint script --config flag"
      pattern: "eslint-config/src/node.ts"
    - from: "apps/frontend/package.json"
      to: "packages/eslint-config/src/react.ts"
      via: "lint script --config flag"
      pattern: "eslint-config/src/react.ts"
---

<objective>
Update consuming apps' lint scripts and validate the full conversion with build, typecheck, and lint.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/.planning/phases/05-eslint-config-typescript-conversion/05-CONTEXT.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/apps/backend/package.json
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller-2/apps/frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update lint scripts in apps to use new .ts config files</name>
  <files>apps/backend/package.json, apps/frontend/package.json</files>
  <read_first>apps/backend/package.json, apps/frontend/package.json</read_first>
  <action>
    Update the lint scripts in both apps to point to the new `.ts` files (per CLEAN-04: apps must import without errors).

    **apps/backend/package.json** - Update the lint script:
    - BEFORE: `"lint": "eslint . --config ../../packages/eslint-config/src/node.js"`
    - AFTER: `"lint": "eslint . --config ../../packages/eslint-config/src/node.ts"`

    **apps/frontend/package.json** - Update the lint script:
    - BEFORE: `"lint": "eslint . --config ../../packages/eslint-config/src/react.js"`
    - AFTER: `"lint": "eslint . --config ../../packages/eslint-config/src/react.ts"`

    Note: ESLint v9+ supports passing `.ts` config files directly via `--config` flag.
    The apps use workspace reference `"@ks0555/eslint-config": "workspace:*"` so they always get the local package.

    Alternative (after tsup build): Could also update to use `dist/node.js` instead of `src/node.ts`.
    But per D-01/D-09 decisions, the immediate conversion uses `.ts` files directly.
    The `dist/` output is for external consumers, not the local workspace apps.
  </action>
  <acceptance_criteria>
    - `grep -c "eslint-config/src/node.ts" apps/backend/package.json` returns 1
    - `grep -c "eslint-config/src/react.ts" apps/frontend/package.json` returns 1
    - `grep -c "node.js" apps/backend/package.json` returns 0 (in lint script)
    - `grep -c "react.js" apps/frontend/package.json` returns 0 (in lint script)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "src/node.ts" apps/backend/package.json && grep -q "src/react.ts" apps/frontend/package.json && echo "PASS: lint scripts updated to .ts files"</automated>
  </verify>
  <done>Lint scripts in both apps updated to reference .ts config files instead of .js</done>
</task>

<task type="auto">
  <name>Task 2: Build and validate eslint-config package</name>
  <files>packages/eslint-config/</files>
  <read_first>packages/eslint-config/package.json, packages/eslint-config/tsup.config.ts</read_first>
  <action>
    Build the eslint-config package using tsup to generate `dist/` output with .js and .d.ts files.

    Steps:
    1. Run `pnpm --filter @ks0555/eslint-config build` to execute tsup
    2. Verify `dist/node.js`, `dist/node.d.ts`, `dist/react.js`, `dist/react.d.ts` exist
    3. Verify the output files are valid ESM (contain `export default`)

    This validates D-06, D-07, D-08: tsup compiles .ts → .js + .d.ts in ESM format to `dist/`.
  </action>
  <acceptance_criteria>
    - `test -f packages/eslint-config/dist/node.js` returns 0
    - `test -f packages/eslint-config/dist/node.d.ts` returns 0
    - `test -f packages/eslint-config/dist/react.js` returns 0
    - `test -f packages/eslint-config/dist/react.d.ts` returns 0
    - `grep -c "export default" packages/eslint-config/dist/node.js` returns 1
    - `grep -c "export default" packages/eslint-config/dist/react.js` returns 1
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @ks0555/eslint-config build && test -f packages/eslint-config/dist/node.js && test -f packages/eslint-config/dist/react.js && echo "PASS: eslint-config built successfully"</automated>
  </verify>
  <done>eslint-config package builds successfully with tsup, generating dist/node.js, dist/node.d.ts, dist/react.js, dist/react.d.ts</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 5 ESLint Config TypeScript Conversion is complete:
    - `node.js` → `node.ts` (ESM export default, import type syntax)
    - `react.js` → `react.ts` (ESM export default, import type syntax)
    - `tsup.config.ts` created for ESM + .d.ts output
    - `package.json` updated with type:module, main/types pointing to dist/
    - Both apps' lint scripts updated to reference .ts files
    - Build completed: dist/ contains .js + .d.ts files
  </what-built>
  <how-to-verify>
    1. Run `pnpm build` from root — should complete with zero errors
    2. Run `pnpm typecheck` from root — should complete with zero errors
    3. Run `pnpm lint` from root — should complete with zero errors
    4. Verify no `.js` files remain in `packages/eslint-config/src/`

    Expected: All three commands pass. If any fail, check the error output for import errors or type errors.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lint scripts → eslint-config | Config path must be valid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | T (Tampering) | apps lint scripts | accept | Scripts point to local workspace files, low risk |
| T-05-06 | I (Information Disclosure) | dist/ output | accept | Only ESLint configs, no sensitive data |
</threat_model>

<verification>
- `pnpm build` completes with exit code 0
- `pnpm typecheck` completes with exit code 0
- `pnpm lint` completes with exit code 0
- No `.js` files remain in `packages/eslint-config/src/`
</verification>

<success_criteria>
- [ ] `pnpm build` passes with zero errors (CLEAN-04)
- [ ] `pnpm typecheck` passes with zero errors (CLEAN-04)
- [ ] `pnpm lint` passes with zero errors (CLEAN-04)
- [ ] Both apps can import eslint-config without errors (CLEAN-04)
- [ ] `packages/eslint-config/dist/` contains .js + .d.ts files
</success_criteria>

<output>
After completion, create `.planning/phases/05-eslint-config-typescript-conversion/05-03-SUMMARY.md`
</output>
