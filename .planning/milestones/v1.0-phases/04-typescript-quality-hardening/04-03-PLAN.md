---
phase: 04-typescript-quality-hardening
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified: []
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03, VAL-04]

must_haves:
  truths:
    - "pnpm build completes with zero errors"
    - "pnpm typecheck passes with zero errors"
    - "pnpm lint passes with zero errors"
  artifacts: []
  key_links:
    - from: "root package.json"
      to: "turbo"
      via: "turbo run build/typecheck/lint"
      pattern: "turbo (build|typecheck|lint)"
---

<objective>
Run the full validation gate — build, typecheck, and lint — to confirm all TypeScript quality changes are correct.

Purpose: Final verification that CLEAN-01 and TS-01/TS-02/TS-03 changes don't break anything. Satisfies VAL-01 through VAL-04.
Output: All three commands passing with zero errors.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-typescript-quality-hardening/04-01-PLAN.md
@.planning/phases/04-typescript-quality-hardening/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build, typecheck, and lint validation gate</name>
  <files>N/A — verification only</files>
  <read_first>package.json</read_first>
  <action>Run the three validation commands from the project root in sequence:

1. `pnpm build` — verifies TypeScript compilation and Vite build succeed for both frontend and backend
2. `pnpm typecheck` — runs `tsc --noEmit` across all workspaces via turbo
3. `pnpm lint` — runs ESLint across all workspaces via turbo

If any command fails, capture the full error output and identify the root cause (missing type, incorrect import, etc.). Fix the issue in the relevant file from Plan 01 or Plan 02 and re-run the failing command.</action>
  <verify>
    <automated>pnpm build && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>All three commands (build, typecheck, lint) complete with exit code 0. VAL-01, VAL-02, VAL-03 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Run TypeScript rules agent verification (VAL-04)</name>
  <files>N/A — verification only</files>
  <read_first>package.json</read_first>
  <action>Run the ts-reviewer agent or equivalent TypeScript quality check across the codebase. This validates that all TypeScript best practices are followed — no deprecation warnings, proper strictness, clean type annotations. If the ts-reviewer agent is not available as a CLI tool, perform a manual sweep:

1. `rg '@ts-ignore|@ts-nocheck|@ts-expect-error' --type tsx apps/ packages/` — should return zero results (no suppressions)
2. `rg 'ignoreDeprecations' --type tsx apps/ packages/` — should return zero results (no deprecation workarounds)
3. Confirm `noUncheckedIndexedAccess` is true in all tsconfig files</action>
  <verify>
    <automated>rg '@ts-ignore|@ts-nocheck|@ts-expect-error' --type tsx apps/ packages/ | grep -v node_modules | grep -v dist | wc -l</automated>
  </verify>
  <done>Zero TypeScript suppressions found. No deprecation workarounds. VAL-04 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only phase — no code changes, only running existing commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Dismission | Mitigation Plan |
|-----------|----------|-----------|------------|-----------------|
| T-04-04 | Availability | Build pipeline | accept | Read-only verification; if build fails, root cause is in prior plans and will be fixed iteratively |
</threat_model>

<verification>
- `pnpm build` exits 0
- `pnpm typecheck` exits 0
- `pnpm lint` exits 0
- Zero `@ts-ignore`/`@ts-nocheck`/`@ts-expect-error` suppressions in codebase
- No `ignoreDeprecations` workarounds in any tsconfig
</verification>

<success_criteria>
All validation gates pass: build (VAL-01), typecheck (VAL-02), lint (VAL-03), and TypeScript rules review (VAL-04). Phase 4 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/04-typescript-quality-hardening/04-03-SUMMARY.md`
</output>
