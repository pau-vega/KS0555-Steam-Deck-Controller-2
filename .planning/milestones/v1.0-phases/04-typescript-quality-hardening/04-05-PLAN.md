---
phase: 04-typescript-quality-hardening
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
gap_closure: true
requirements: [CLEAN-01]
user_setup: []

must_haves:
  truths:
    - "Running `find apps/frontend/src -name '*.js'` returns zero results — all 13 JavaScript files have been deleted from disk"
    - "No untracked .js files remain in apps/frontend/src/ (git status shows clean)"
  artifacts:
    - path: "apps/frontend/src/"
      provides: "JS-free source directory on disk"
      check: "find apps/frontend/src -name '*.js' -not -path '*/node_modules/*' returns 0 results"
  key_links:
    - from: "git status"
      to: "apps/frontend/src/*.js"
      via: "files show as ?? (untracked)"
      pattern: "\\?\\? .*\\.js$"
---

<objective>
Delete 13 untracked JavaScript files from apps/frontend/src/ that remained on disk after git deletion.

Purpose: Close UAT gap #1 — JS files were removed from git index but not from disk in commit e985693, and follow-up commit a4218ca did not fully clean them up.

Output: Clean apps/frontend/src/ with zero .js files on disk.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-typescript-quality-hardening/04-UAT.md
@.planning/phases/04-typescript-quality-hardening/04-01-SUMMARY.md

Gap root cause: JS files deleted from git in e985693 but remained on disk as untracked files.
Follow-up commit a4218ca ("actually delete") only added vite.config.js and vitest.config.js — did not remove the original JS files from disk.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete untracked JS files from apps/frontend/src/</name>
  <files>apps/frontend/src/app.js, apps/frontend/src/App.test.js, apps/frontend/src/components/control-pad.js, apps/frontend/src/components/control-pad.test.js, apps/frontend/src/components/status-bar.js, apps/frontend/src/components/status-bar.test.js, apps/frontend/src/hooks/use-gamepad.js, apps/frontend/src/hooks/use-gamepad.test.js, apps/frontend/src/hooks/use-websocket.js, apps/frontend/src/hooks/use-websocket.test.js, apps/frontend/src/main.js, apps/frontend/src/setupTests.js, apps/frontend/src/types.js</files>
  <action>Delete all 13 untracked JS files from apps/frontend/src/ that have TypeScript equivalents.

These files show as `??` (untracked) in git status — they were "deleted" from git index but remained on disk.

Run the following commands to delete them:

```bash
rm apps/frontend/src/app.js
rm apps/frontend/src/App.test.js
rm apps/frontend/src/components/control-pad.js
rm apps/frontend/src/components/control-pad.test.js
rm apps/frontend/src/components/status-bar.js
rm apps/frontend/src/components/status-bar.test.js
rm apps/frontend/src/hooks/use-gamepad.js
rm apps/frontend/src/hooks/use-gamepad.test.js
rm apps/frontend/src/hooks/use-websocket.js
rm apps/frontend/src/hooks/use-websocket.test.js
rm apps/frontend/src/main.js
rm apps/frontend/src/setupTests.js
rm apps/frontend/src/types.js
```

Alternative: `cd apps/frontend/src && git clean -fd` (removes all untracked files in that directory)

Verify each file has a TypeScript equivalent before deletion:
- app.js → app.tsx
- App.test.js → App.test.tsx
- components/control-pad.js → components/control-pad.tsx
- components/control-pad.test.js → components/control-pad.test.tsx
- components/status-bar.js → components/status-bar.tsx
- components/status-bar.test.js → components/status-bar.test.tsx
- hooks/use-gamepad.js → hooks/use-gamepad.ts
- hooks/use-gamepad.test.js → hooks/use-gamepad.test.ts
- hooks/use-websocket.js → hooks/use-websocket.ts
- hooks/use-websocket.test.js → hooks/use-websocket.test.ts
- main.js → main.tsx
- setupTests.js → setupTests.ts
- types.js → types.ts

Gap reason: Commit e985693 used `git rm` which removed files from git index but not disk. Follow-up commit a4218ca did not clean up these files.
</action>
  <verify>
    <automated>find apps/frontend/src -name '*.js' -not -path '*/node_modules/*' -not -path '*/dist/*' | wc -l</automated>
    <automated>git status --short apps/frontend/src/*.js apps/frontend/src/**/*.js 2>/dev/null | grep -c "^??" || echo "0"</automated>
  </verify>
  <done>Zero .js files remain on disk in apps/frontend/src/. All 13 untracked JS files deleted. `git status` shows no ?? files in apps/frontend/src/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan deletes dead code files that are untracked — no trust boundaries affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Integrity | File deletion | accept | Only deleting .js files that have confirmed .ts/.tsx equivalents; all files are untracked (??) so no git history is lost |
</threat_model>

<verification>
- `find apps/frontend/src -name '*.js' -not -path '*/node_modules/*'` returns zero results
- `git status` shows no untracked files in apps/frontend/src/ (no lines with `??`)
- All 13 JS files confirmed deleted from disk
</verification>

<success_criteria>
All 13 untracked JS files deleted from apps/frontend/src/ on disk. CLEAN-01 fully satisfied — no JS files remain anywhere in source directory.
</success_criteria>

<output>
After completion, create `.planning/phases/04-typescript-quality-hardening/04-05-SUMMARY.md`
</output>
