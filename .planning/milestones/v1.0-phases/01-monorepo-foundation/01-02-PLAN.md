---
phase: 01-monorepo-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - apps/backend/package.json
  - apps/backend/tsconfig.json
  - apps/backend/src/index.ts
  - apps/backend/vitest.config.ts
  - apps/backend/src/index.test.ts
autonomous: true
requirements: [MONO-01, MONO-03, MONO-04]
user_setup: []
must_haves:
  truths:
    - Backend app has Fastify installed and working
    - TypeScript config extends shared packages/tsconfig
    - ESLint extends shared packages/eslint-config
    - Vitest is configured for testing
    - `pnpm dev` starts backend with tsx watch
  artifacts:
    - path: "apps/backend/package.json"
      provides: "Backend package definition with Fastify, Vitest"
      contains: '"fastify":'
    - path: "apps/backend/tsconfig.json"
      provides: "TypeScript config extending shared tsconfig"
      contains: '"extends": "@ks0555/tsconfig/tsconfig.node.json"'
    - path: "apps/backend/src/index.ts"
      provides: "Fastify server with basic route"
      contains: "fastify"
    - path: "apps/backend/vitest.config.ts"
      provides: "Vitest configuration"
      contains: "vitest"
  key_links:
    - from: "apps/backend/tsconfig.json"
      to: "packages/tsconfig/tsconfig.node.json"
      via: "extends field"
      pattern: '"extends": ".*@ks0555/tsconfig.*"'
    - from: "apps/backend/package.json"
      to: "@ks0555/eslint-config"
      via: "eslint config extends"
      pattern: "eslint-config"
---

<objective>
Set up backend app with Fastify, TypeScript, ESLint, and Vitest.
</objective>

<purpose>
Creates the backend workspace with Fastify framework (D-01), TypeScript extending shared config (D-02), and testing infrastructure (D-08).
</purpose>

<output>
- apps/backend/package.json (updated)
- apps/backend/tsconfig.json (updated)
- apps/backend/src/index.ts (updated with Fastify)
- apps/backend/vitest.config.ts
- apps/backend/src/index.test.ts
</output>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/PROJECT.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/ROADMAP.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/STATE.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/phases/01-monorepo-foundation/01-CONTEXT.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/phases/01-monorepo-foundation/01-RESEARCH.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/backend/package.json
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/backend/tsconfig.json
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/backend/src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update backend package.json with Fastify and Vitest</name>
  <read_first>
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/backend/package.json
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/phases/01-monorepo-foundation/01-CONTEXT.md (D-01, D-08)
  </read_first>
  <files>apps/backend/package.json</files>
  <behavior>
    - Test 1: package.json contains "fastify" dependency
    - Test 2: package.json contains "vitest" devDependency
    - Test 3: package.json contains "@ks0555/eslint-config" devDependency
    - Test 4: "lint" script uses eslint . --config packages/eslint-config/src/node.ts
  </behavior>
  <action>
    Update apps/backend/package.json to:

    ```json
    {
      "name": "@ks0555/backend",
      "version": "0.1.0",
      "private": true,
      "type": "module",
      "scripts": {
        "dev": "tsx watch src/index.ts",
        "start": "tsx src/index.ts",
        "typecheck": "tsc --noEmit",
        "lint": "eslint . --config ../../packages/eslint-config/src/node.ts",
        "test": "vitest run"
      },
      "dependencies": {
        "fastify": "^5.0.0",
        "serialport": "^12.0.0",
        "@fastify/websocket": "^9.0.0",
        "ws": "^8.18.0"
      },
      "devDependencies": {
        "@ks0555/eslint-config": "workspace:*",
        "@ks0555/tsconfig": "workspace:*",
        "@types/node": "^22.10.0",
        "@types/ws": "^8.5.13",
        "tsx": "^4.19.0",
        "typescript": "^5.7.0",
        "vitest": "^4.1.4",
        "@vitest/coverage-v8": "^4.1.4"
      }
    }
    ```

    Key changes (per D-01, D-06, D-08):
    - Add "fastify" to dependencies
    - Add "@fastify/websocket" to dependencies (D-06: install now for Phase 2)
    - Add "vitest" and "@vitest/coverage-v8" to devDependencies (D-08)
    - Add "@ks0555/eslint-config": "workspace:*" and "@ks0555/tsconfig": "workspace:*" dependencies
    - Update "lint" script to use shared eslint-config
    - Add "test" script
  </action>
  <acceptance_criteria>
    - apps/backend/package.json exists and contains "fastify": "^5.0.0"
    - apps/backend/package.json contains "vitest": "^4.1.4"
    - apps/backend/package.json contains "@ks0555/eslint-config": "workspace:*"
    - apps/backend/package.json contains "test": "vitest run"
    - apps/backend/package.json contains "lint": "eslint . --config ../../packages/eslint-config/src/node.ts"
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"fastify"' apps/backend/package.json && grep -q '"vitest"' apps/backend/package.json && grep -q '"@ks0555/eslint-config"' apps/backend/package.json && echo "Backend package.json updated correctly"</automated>
  </verify>
  <done>Backend package.json updated with Fastify, Vitest, and shared configs</done>
</task>

<task type="auto">
  <name>Task 2: Update backend tsconfig to extend shared config</name>
  <read_first>
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/backend/tsconfig.json
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/phases/01-monorepo-foundation/01-CONTEXT.md (D-02)
  </read_first>
  <files>apps/backend/tsconfig.json</files>
  <action>
    Update apps/backend/tsconfig.json to extend shared tsconfig (per D-02):

    ```json
    {
      "extends": "@ks0555/tsconfig/tsconfig.node.json",
      "compilerOptions": {
        "outDir": "dist",
        "rootDir": "src"
      },
      "include": ["src"],
      "exclude": ["node_modules", "dist"]
    }
    ```

    Key changes:
    - Add "extends": "@ks0555/tsconfig/tsconfig.node.json" (D-02)
    - Remove redundant compilerOptions that are already in base config (strict, esModuleInterop, etc.)
    - Keep outDir, rootDir, include, exclude as they are project-specific
  </action>
  <acceptance_criteria>
    - apps/backend/tsconfig.json contains "extends": "@ks0555/tsconfig/tsconfig.node.json"
    - apps/backend/tsconfig.json does NOT contain "strict": true (comes from base)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"extends": "@ks0555/tsconfig/tsconfig.node.json"' apps/backend/tsconfig.json && echo "Backend tsconfig extends shared config"</automated>
  </verify>
  <done>Backend tsconfig extends shared packages/tsconfig/tsconfig.node.json</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Fastify server and test</name>
  <read_first>
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/backend/src/index.ts
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/phases/01-monorepo-foundation/01-CONTEXT.md (D-03: PORT=3001 default)
  </read_first>
  <files>apps/backend/src/index.ts, apps/backend/src/index.test.ts, apps/backend/vitest.config.ts</files>
  <behavior>
    - Test 1: GET / returns { hello: 'world' }
    - Test 2: Server listens on PORT env var or default 3001
    - Test 3: Fastify instance is properly typed
  </behavior>
  <action>
    1. Update apps/backend/src/index.ts with Fastify server (per D-01, D-03, D-04, D-05):

    ```typescript
    import fastify, { type FastifyInstance } from 'fastify'

    const server: FastifyInstance = fastify({ logger: true })

    server.get('/', async () => {
      return { hello: 'world' }
    })

    const start = async (): Promise<void> => {
      try {
        const port = parseInt(process.env.PORT || '3001', 10)
        await server.listen({ port, host: '0.0.0.0' })
        server.log.info(`Server listening on port ${port}`)
      } catch (err) {
        server.log.error(err)
        process.exit(1)
      }
    }

    if (import.meta.url === `file://${process.argv[1]}`) {
      start()
    }

    export default server
    ```

    2. Create apps/backend/vitest.config.ts:

    ```typescript
    import { defineConfig } from 'vitest/config'

    export default defineConfig({
      test: {
        environment: 'node',
        include: ['src/**/*.test.ts']
      }
    })
    ```

    3. Create apps/backend/src/index.test.ts:

    ```typescript
    import { describe, it, expect } from 'vitest'
    import build from './index.js'

    describe('Backend server', () => {
      it('should return hello world on GET /', async () => {
        const app = build()
        const response = await app.inject({
          method: 'GET',
          url: '/'
        })
        expect(response.statusCode).toBe(200)
        expect(JSON.parse(response.payload)).toEqual({ hello: 'world' })
        await app.close()
      })
    })
    ```
  </action>
  <acceptance_criteria>
    - apps/backend/src/index.ts contains "import fastify" and "fastify({ logger: true })"
    - apps/backend/src/index.ts uses process.env.PORT with default 3001 (D-03)
    - apps/backend/vitest.config.ts exists with vitest config
    - apps/backend/src/index.test.ts exists with at least one test
    - `cd apps/backend && vitest run` passes (or `pnpm test` from backend directory)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && test -f src/index.ts && test -f vitest.config.ts && test -f src/index.test.ts && grep -q 'fastify' src/index.ts && echo "Backend server files created"</automated>
  </verify>
  <done>Fastify server created with basic route and Vitest tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→backend | HTTP requests to Fastify server (port 3001) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Spoofing | Fastify server | accept | Local development server only, no auth required for MVP |
| T-01-04 | Tampering | HTTP requests | accept | JSON schema validation deferred to Phase 2 when WebSocket implemented |
| T-01-05 | Information Disclosure | Fastify logger | accept | Logger enabled for development, disable in production via env var |
| T-01-06 | Denial of Service | Fastify server | accept | No rate limiting for MVP, single-user local device |
| T-01-07 | Elevation of Privilege | N/A | accept | No auth system for MVP, local device only |
</threat_model>

<verification>
- `cd apps/backend && grep -q '"fastify"' package.json`
- `cd apps/backend && grep -q '"extends": "@ks0555/tsconfig/tsconfig.node.json"' tsconfig.json`
- `cd apps/backend && vitest run` passes (or `pnpm --filter backend test`)
- `pnpm typecheck` passes for backend
</verification>

<success_criteria>
- [ ] Backend has Fastify installed and working (GET / returns { hello: 'world' })
- [ ] TypeScript config extends @ks0555/tsconfig/tsconfig.node.json (D-02)
- [ ] Vitest is configured with at least one passing test (D-08)
- [ ] Lint script uses shared eslint-config node preset (D-07)
- [ ] `pnpm --filter backend dev` starts server with tsx watch (D-04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-foundation/01-02-SUMMARY.md`
</output>
