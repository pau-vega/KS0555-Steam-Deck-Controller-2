---
phase: 01-monorepo-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01", "02", "03"]
files_modified:
  - turbo.json
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [MONO-02, MONO-04]
user_setup: []
must_haves:
  truths:
    - `pnpm install` works from root
    - `pnpm dev` starts both backend and frontend
    - `pnpm typecheck` runs typecheck in all workspaces
    - `pnpm lint` runs linting in all workspaces
    - `pnpm build` builds all workspaces
  artifacts:
    - path: "turbo.json"
      provides: "Turbo pipeline configuration"
      contains: '"cache": false'
    - path: "package.json"
      provides: "Root package with workspace scripts"
      contains: '"turbo dev"'
  key_links:
    - from: "package.json"
      to: "turbo.json"
      via: "pnpm run dev calls turbo"
      pattern: "turbo dev"
    - from: "apps/backend"
      to: "turbo.json"
      via: "dependsOn pipeline"
      pattern: "dev.*dependsOn"
    - from: "apps/frontend"
      to: "turbo.json"
      via: "dependsOn pipeline"
      pattern: "dev.*dependsOn"
---

<objective>
Set up root turbo.json and update root package.json scripts for monorepo orchestration.
</objective>

<purpose>
Configures Turbo pipeline (D-20, D-21) and root scripts so `pnpm dev` starts all apps in parallel.
</purpose>

<output>
- turbo.json (new file)
- package.json (updated scripts)
</output>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/PROJECT.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/ROADMAP.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/STATE.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/phases/01-monorepo-foundation/01-CONTEXT.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/phases/01-monorepo-foundation/01-RESEARCH.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create turbo.json pipeline</name>
  <read_first>
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/phases/01-monorepo-foundation/01-CONTEXT.md (D-20, D-21)
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/codebase/ARCHITECTURE.md (existing Turbo patterns)
  </read_first>
  <files>turbo.json</files>
  <action>
    Create turbo.json at root (per D-20, D-21):

    ```json
    {
      "$schema": "https://turbo.build/schema.json",
      "pipeline": {
        "dev": {
          "cache": false,
          "persistent": true,
          "dependsOn": ["^dev"]
        },
        "build": {
          "dependsOn": ["^build"],
          "outputs": ["dist/**"]
        },
        "typecheck": {
          "dependsOn": ["^typecheck"]
        },
        "lint": {
          "dependsOn": ["^lint"]
        },
        "test": {
          "dependsOn": ["^test"],
          "outputs": []
        }
      }
    }
    ```

    Key settings (per D-21 - parallel execution):
    - "dev": cache: false, persistent: true (for watch mode), dependsOn: ["^dev"]
    - "build": dependsOn workspace dependencies, outputs dist/**
    - "typecheck", "lint", "test": standard pipeline tasks
  </action>
  <acceptance_criteria>
    - turbo.json exists at root
    - turbo.json contains `"cache": false` for dev task (D-21: parallel execution)
    - turbo.json contains `"persistent": true` for dev task
    - turbo.json contains pipeline for dev, build, typecheck, lint, test
  </acceptance_criteria>
  <verify>
    <automated>test -f turbo.json && grep -q '"cache": false' turbo.json && grep -q '"persistent": true' turbo.json && echo "turbo.json created with correct pipeline"</automated>
  </verify>
  <done>Turbo pipeline configured with dev (persistent), build, typecheck, lint, test tasks</done>
</task>

<task type="auto">
  <name>Task 2: Update root package.json scripts</name>
  <read_first>
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/package.json
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/phases/01-monorepo-foundation/01-CONTEXT.md (D-20)
  </read_first>
  <files>package.json</files>
  <action>
    Update root package.json scripts (per D-20):

    ```json
    {
      "name": "ks0555-steam-deck-controller",
      "version": "0.1.0",
      "private": true,
      "description": "Steam Deck Bluetooth robot controller — monorepo with React frontend and Node.js backend",
      "scripts": {
        "dev": "turbo dev",
        "dev:frontend": "pnpm --filter frontend dev",
        "dev:backend": "pnpm --filter backend dev",
        "build": "turbo build",
        "typecheck": "turbo typecheck",
        "lint": "turbo lint",
        "test": "turbo test"
      },
      "engines": {
        "node": ">=18.0.0"
      },
      "packageManager": "pnpm@10.29.3"
    }
    ```

    Key changes (per D-20):
    - Change "dev" script from "pnpm --filter backend dev & pnpm --filter frontend dev" to "turbo dev"
    - Add "build": "turbo build"
    - Add "typecheck": "turbo typecheck"
    - Add "lint": "turbo lint"
    - Add "test": "turbo test"
    - Keep "dev:frontend" and "dev:backend" for individual app dev
  </action>
  <acceptance_criteria>
    - package.json contains `"dev": "turbo dev"` (D-20)
    - package.json contains `"build": "turbo build"`
    - package.json contains `"typecheck": "turbo typecheck"`
    - package.json contains `"lint": "turbo lint"`
    - package.json contains `"test": "turbo test"`
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"dev": "turbo dev"' package.json && grep -q '"build": "turbo build"' package.json && grep -q '"typecheck": "turbo typecheck"' package.json && echo "Root package.json updated with turbo scripts"</automated>
  </verify>
  <done>Root package.json updated with turbo orchestration scripts</done>
</task>

<task type="auto">
  <name>Task 3: Install dependencies and verify</name>
  <read_first>
    - @/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/phases/01-monorepo-foundation/01-CONTEXT.md
  </read_first>
  <files>pnpm-lock.yaml</files>
  <action>
    Run pnpm install to:
    1. Install all dependencies including new packages (fastify, tailwindcss, vitest, etc.)
    2. Create/udpate pnpm-lock.yaml
    3. Link workspace packages (@ks0555/tsconfig, @ks0555/eslint-config)

    Command: `pnpm install --frozen-lockfile`

    Then verify:
    1. `pnpm typecheck` passes (TypeScript compiles in all workspaces)
    2. `pnpm lint` passes (ESLint works in all workspaces)
    3. `pnpm test` passes (Vitest works in all workspaces)

    Note: If --frozen-lockfile fails (first run), run `pnpm install` without it to generate lockfile.
  </action>
  <acceptance_criteria>
    - `pnpm install` completes without errors
    - `pnpm typecheck` passes (exit code 0)
    - `pnpm lint` passes (exit code 0)
    - Workspace packages are linked (node_modules/@ks0555/ points to packages/)
  </acceptance_criteria>
  <verify>
    <automated>pnpm install --frozen-lockfile 2>&1 | tail -5 && pnpm typecheck 2>&1 | tail -3 && echo "Install and typecheck passed"</automated>
  </verify>
  <done>Dependencies installed, typecheck and lint pass, monorepo foundation complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Root configuration files only - no runtime trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Tampering | turbo.json | accept | Configuration file committed to git |
| T-01-14 | Tampering | pnpm-lock.yaml | accept | Lockfile ensures reproducible builds |
| T-01-15 | Information Disclosure | package.json versions | accept | No secrets in package.json |
</threat_model>

<verification>
- `test -f turbo.json && grep -q 'turbo dev' package.json`
- `pnpm install --frozen-lockfile` passes (or `pnpm install` if first run)
- `pnpm typecheck` passes
- `pnpm lint` passes
</verification>

<success_criteria>
- [ ] turbo.json exists with dev, build, typecheck, lint, test pipeline
- [ ] Root package.json has "dev": "turbo dev" script (D-20)
- [ ] `pnpm install` completes without errors
- [ ] `pnpm typecheck` passes (MONO-03)
- [ ] `pnpm lint` passes
- [ ] `pnpm dev` starts both frontend and backend (MONO-02, MONO-04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-foundation/01-04-SUMMARY.md`
</output>
