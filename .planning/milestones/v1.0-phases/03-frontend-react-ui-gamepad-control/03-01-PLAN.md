---
phase: 03-frontend-react-ui-gamepad-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/index.css
  - apps/frontend/src/app.tsx
  - apps/frontend/src/app.css
  - apps/frontend/src/components/control-pad.tsx
  - apps/frontend/src/components/control-pad.css
  - apps/frontend/src/components/status-bar.tsx
  - apps/frontend/src/components/status-bar.css
  - apps/frontend/.env
autonomous: true
requirements: [FRONT-01, FRONT-02, FRONT-03]
user_setup: []
must_haves:
  truths:
    - "UI displays connection status that updates when WebSocket connects/disconnects (FRONT-01)"
    - "Manual buttons (F, B, L, R, S) send commands via WebSocket when clicked (FRONT-02)"
    - "Last sent command is displayed on screen (FRONT-03)"
  artifacts:
    - path: "apps/frontend/src/index.css"
      provides: "Tailwind CSS v4 import and @theme dark color tokens"
      contains: "@import 'tailwindcss'"
    - path: "apps/frontend/src/app.tsx"
      provides: "Main App component with WebSocket URL from env, last command display"
      contains: "import.meta.env.VITE_WS_URL"
    - path: "apps/frontend/src/components/control-pad.tsx"
      provides: "Manual control buttons with Tailwind classes"
      contains: "className="
    - path: "apps/frontend/src/components/status-bar.tsx"
      provides: "Connection status display showing Backend + Gamepad"
      contains: "Backend"
  key_links:
    - from: "apps/frontend/src/app.tsx"
      to: "import.meta.env.VITE_WS_URL"
      via: "useWebSocket hook reads env internally per D-06"
      pattern: "VITE_WS_URL"
    - from: "apps/frontend/src/components/status-bar.tsx"
      to: "Backend label"
      via: "StatusBar displays 'Backend' instead of 'WebSocket' per D-08"
      pattern: "Backend"
---

<objective>
Migrate frontend styling to Tailwind CSS v4, configure environment-based WebSocket URL, and update status bar labels.

Purpose: Apply Tailwind utilities throughout the frontend (D-01, D-02, D-03), configure WebSocket URL via Vite env variable (D-04, D-05, D-06, D-07), and update status display to show "Backend" instead of "WebSocket" (D-08, D-09, D-10, D-11).

Output: Clean Tailwind-based UI with env-configured WebSocket URL and proper status labels.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/PROJECT.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/ROADMAP.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/REQUIREMENTS.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/phases/03-frontend-react-ui-gamepad-control/03-CONTEXT.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/src/app.tsx
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/src/hooks/use-websocket.ts
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/src/components/control-pad.tsx
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/src/components/status-bar.tsx
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/src/index.css
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/src/app.css
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/src/types.ts
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/vite.config.ts
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Tailwind CSS v4 with custom theme colors</name>
  <files>apps/frontend/src/index.css</files>
  <read_first>
    - apps/frontend/src/index.css (current: only `@import 'tailwindcss'`)
    - apps/frontend/src/app.css (current dark theme colors to preserve)
    - 03-CONTEXT.md (D-02: Use Tailwind v4 @theme directive with custom colors)
  </read_first>
  <action>
    1. Update `apps/frontend/src/index.css` to include @theme directive with custom dark theme colors per D-02 and specifics section:
       ```css
       @import 'tailwindcss';
       
       @theme {
         --color-background: #1a1a2e;
         --color-surface: #16213e;
         --color-border: #0f3460;
         --color-accent: #e94560;
         --color-success: #1b4332;
         --color-success-text: #95d5b2;
         --color-error: #3d0000;
         --color-error-text: #ff6b6b;
       }
       ```
    2. Add base reset styles and body styling using Tailwind @layer:
       ```css
       @layer base {
         body {
           font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
           background-color: var(--color-background);
           color: #eee;
           min-height: 100vh;
           display: flex;
           justify-content: center;
           align-items: center;
         }
       }
       ```
    3. Delete `apps/frontend/src/app.css` (per D-01, D-03: remove all component CSS files)
  </action>
  <acceptance_criteria>
    - `apps/frontend/src/index.css` contains `@import 'tailwindcss'` and `@theme` block with all 8 custom color variables
    - `apps/frontend/src/index.css` contains `@layer base` with body styles
    - `apps/frontend/src/app.css` does NOT exist (deleted)
    - `grep -c "1a1a2e\|16213e\|0f3460\|e94560" apps/frontend/src/index.css` returns 4 (all colors present)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>index.css configured with Tailwind v4 @theme directive and custom dark theme colors; app.css removed</done>
</task>

<task type="auto">
  <name>Task 2: Update App.tsx with env-based WebSocket URL and remove app.css import</name>
  <files>apps/frontend/src/app.tsx</files>
  <read_first>
    - apps/frontend/src/app.tsx (current: imports app.css, hardcodes ws://localhost:8080)
    - apps/frontend/src/hooks/use-websocket.ts (must read env internally per D-06)
    - 03-CONTEXT.md (D-04, D-05, D-06: VITE_WS_URL env var, default port 3001, hook reads env internally)
  </read_first>
  <action>
    1. Remove `import "./app.css";` line from app.tsx (line 6) since we're using Tailwind now
    2. Update useWebSocket call to remove URL parameter (per D-06: hook reads env internally):
       - Change: `const { connected: wsConnected, send, autoReconnect } = useWebSocket("ws://localhost:8080");`
       - To: `const { connected: wsConnected, send, autoReconnect } = useWebSocket();`
    3. Update last command display section to use Tailwind classes instead of CSS:
       - Change: `<div className="last-command">` to `<div className="text-lg p-3 bg-surface rounded-lg border border-border">`
       - Change: `<strong>{lastCommand}</strong>` to `<strong className="text-accent text-xl">{lastCommand}</strong>`
  </action>
  <acceptance_criteria>
    - `apps/frontend/src/app.tsx` does NOT import `./app.css`
    - `apps/frontend/src/app.tsx` calls `useWebSocket()` with NO arguments
    - `apps/frontend/src/app.tsx` last command div uses Tailwind classes: `text-lg p-3 bg-surface rounded-lg border border-border`
    - `apps/frontend/src/app.tsx` strong tag uses: `text-accent text-xl`
    - `grep -c "app.css" apps/frontend/src/app.tsx` returns 0
    - `grep -c "useWebSocket()" apps/frontend/src/app.tsx` returns 1
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | grep -i "error" | wc -l</automated>
  </verify>
  <done>App.tsx updated: removed CSS import, useWebSocket called without URL param, last command display uses Tailwind classes</done>
</task>

<task type="auto">
  <name>Task 3: Update useWebSocket hook to read env internally and add connecting state</name>
  <files>apps/frontend/src/hooks/use-websocket.ts</files>
  <read_first>
    - apps/frontend/src/hooks/use-websocket.ts (current: takes url parameter)
    - 03-CONTEXT.md (D-06: hook reads import.meta.env.VITE_WS_URL internally, D-11: show "Connecting..." state)
  </read_first>
  <action>
    1. Remove `url` parameter from useWebSocket function (no longer takes arguments)
    2. Read WebSocket URL from env inside the hook:
       ```typescript
       const WS_URL = import.meta.env.VITE_WS_URL || 'ws://localhost:3001';
       ```
    3. Add `connecting` state for "Connecting..." status (per D-11):
       ```typescript
       const [connecting, setConnecting] = useState(false);
       ```
    4. Update connect function to set connecting state:
       - Set `connecting = true` when starting connection
       - Set `connecting = false` in onopen and onerror handlers
    5. Update reconnect logic to use `WS_URL` constant instead of `url` parameter
    6. Return `connecting` state along with `connected` and other values
    7. Delete `apps/frontend/src/hooks/use-websocket.js` if it exists (cleanup .js files per CONTEXT)
  </action>
  <acceptance_criteria>
    - `apps/frontend/src/hooks/use-websocket.ts` does NOT take url parameter in function signature
    - `apps/frontend/src/hooks/use-websocket.ts` reads `import.meta.env.VITE_WS_URL` internally
    - `apps/frontend/src/hooks/use-websocket.ts` has `connecting` state that is `true` during connection attempt
    - `apps/frontend/src/hooks/use-websocket.ts` returns `{ connected, connecting, send, autoReconnect }`
    - `grep -c "import.meta.env.VITE_WS_URL" apps/frontend/src/hooks/use-websocket.ts` returns 1
    - `grep -c "WS_URL" apps/frontend/src/hooks/use-websocket.ts` returns at least 2 (definition + usage)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | grep -i "error" | wc -l</automated>
  </verify>
  <done>useWebSocket hook reads VITE_WS_URL from env internally, has connecting state, no longer takes url parameter</done>
</task>

<task type="auto">
  <name>Task 4: Migrate ControlPad to Tailwind and delete CSS</name>
  <files>apps/frontend/src/components/control-pad.tsx, apps/frontend/src/components/control-pad.css</files>
  <read_first>
    - apps/frontend/src/components/control-pad.tsx (current: imports control-pad.css, uses CSS classes)
    - apps/frontend/src/components/control-pad.css (current: grid layout, button styles)
    - 03-CONTEXT.md (D-01, D-03: full Tailwind migration, remove CSS files)
    - apps/frontend/src/types.ts (Direction type)
  </read_first>
  <action>
    1. Remove `import "./control-pad.css";` from control-pad.tsx
    2. Update control-pad div to use Tailwind classes:
       ```tsx
       <div className="grid grid-cols-3 grid-rows-3 gap-2 w-fit mx-auto">
       ```
    3. Update button elements to use Tailwind classes instead of CSS:
       - Base button: `className={`w-20 h-20 rounded-xl text-2xl cursor-pointer transition-all duration-100 flex items-center justify-center border-2 ${disabled ? 'opacity-40 cursor-not-allowed' : 'hover:bg-surface hover:border-accent active:bg-accent active:scale-95'} ${command === 'S' ? 'bg-error border-accent' : 'bg-surface border-border'}`}`
       - Apply gridArea via style prop (keep existing gridArea logic)
    4. Delete `apps/frontend/src/components/control-pad.css` (per D-01, D-03)
    5. Delete `apps/frontend/src/components/control-pad.js` if it exists (cleanup .js files)
  </action>
  <acceptance_criteria>
    - `apps/frontend/src/components/control-pad.tsx` does NOT import `./control-pad.css`
    - `apps/frontend/src/components/control-pad.tsx` uses Tailwind classes: `grid grid-cols-3 grid-rows-3 gap-2`
    - `apps/frontend/src/components/control-pad.tsx` button uses: `w-20 h-20 rounded-xl text-2xl`
    - `apps/frontend/src/components/control-pad.tsx` Stop button (S) has: `bg-error border-accent`
    - `apps/frontend/src/components/control-pad.css` does NOT exist
    - `grep -c "control-pad.css" apps/frontend/src/components/control-pad.tsx` returns 0
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | grep -i "error" | wc -l</automated>
  </verify>
  <done>ControlPad migrated to Tailwind classes, control-pad.css deleted, .js files cleaned up</done>
</task>

<task type="auto">
  <name>Task 5: Migrate StatusBar to Tailwind with "Backend" label and delete CSS</name>
  <files>apps/frontend/src/components/status-bar.tsx, apps/frontend/src/components/status-bar.css</files>
  <read_first>
    - apps/frontend/src/components/status-bar.tsx (current: imports status-bar.css, shows "WebSocket" and "Gamepad")
    - apps/frontend/src/components/status-bar.css (current: status-bar styles)
    - 03-CONTEXT.md (D-08: label as "Backend", D-09: show 2 indicators, D-10: colored pill badge style)
  </read_first>
  <action>
    1. Remove `import "./status-bar.css";` from status-bar.tsx
    2. Update status bar div to use Tailwind classes:
       ```tsx
       <div className="flex gap-4 w-full justify-center">
       ```
    3. Update status items to use Tailwind pill badge style:
       - Connected: `className={`px-3 py-1 rounded-full text-sm font-medium ${wsConnected ? 'bg-success text-success-text' : 'bg-error text-error-text'}`}`
       - Change label from "WebSocket" to "Backend" (per D-08)
       - Gamepad status keeps same logic but with Tailwind: `className={`px-3 py-1 rounded-full text-sm font-medium ${gamepadConnected ? 'bg-success text-success-text' : 'bg-error text-error-text'}`}`
    4. Update app.tsx to pass `connecting` state to StatusBar:
       - Update StatusBar props interface to accept `connecting?: boolean`
       - When connecting, show "Connecting..." text (per D-11)
    5. Delete `apps/frontend/src/components/status-bar.css` (per D-01, D-03)
    6. Delete `apps/frontend/src/components/status-bar.js` if it exists (cleanup .js files)
  </action>
  <acceptance_criteria>
    - `apps/frontend/src/components/status-bar.tsx` does NOT import `./status-bar.css`
    - `apps/frontend/src/components/status-bar.tsx` displays "Backend" (not "WebSocket") per D-08
    - `apps/frontend/src/components/status-bar.tsx` uses Tailwind pill badge: `px-3 py-1 rounded-full text-sm font-medium`
    - `apps/frontend/src/components/status-bar.tsx` shows "Connecting..." when wsConnected is false but connecting is true
    - `apps/frontend/src/components/status-bar.css` does NOT exist
    - `grep -c "Backend" apps/frontend/src/components/status-bar.tsx` returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | grep -i "error" | wc -l</automated>
  </verify>
  <done>StatusBar migrated to Tailwind, shows "Backend" label, "Connecting..." state added, status-bar.css deleted</done>
</task>

<task type="auto">
  <name>Task 6: Create .env file and clean up remaining .js files</name>
  <files>apps/frontend/.env, apps/frontend/src/main.tsx, apps/frontend/src/types.js, apps/frontend/src/app.js</files>
  <read_first>
    - 03-CONTEXT.md (D-07: create .env with VITE_WS_URL=ws://localhost:3001)
    - apps/frontend/src/main.tsx (entry point, check for .js imports)
    - apps/frontend/package.json (dependencies check)
  </read_first>
  <action>
    1. Create `apps/frontend/.env` file with content:
       ```
       VITE_WS_URL=ws://localhost:3001
       ```
       (per D-07, using port 3001 to match Phase 1 D-03 and backend default)
    2. Delete `apps/frontend/src/types.js` (keep types.ts, per CONTEXT cleanup)
    3. Delete `apps/frontend/src/app.js` (keep app.tsx)
    4. Delete `apps/frontend/src/main.js` if exists (keep main.tsx)
    5. Verify no .js files remain in src directory except possibly setupTests.js
    6. Ensure `.env` is in `.gitignore` or create entry if needed (Vite automatically ignores .env files)
  </action>
  <acceptance_criteria>
    - `apps/frontend/.env` exists with content `VITE_WS_URL=ws://localhost:3001`
    - `apps/frontend/src/types.js` does NOT exist
    - `apps/frontend/src/app.js` does NOT exist
    - `grep -r "VITE_WS_URL=ws://localhost:3001" apps/frontend/.env` returns 1
    - `ls apps/frontend/src/**/*.js 2>/dev/null | wc -l` returns 0 (or only setupTests.js)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx vite build 2>&1 | tail -10</automated>
  </verify>
  <done>.env file created with WebSocket URL, all .js build artifacts cleaned up from src directory</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Frontend UI | User interacts with React UI, gamepad input crosses here |
| Frontend → Backend WebSocket | Commands sent to backend via WebSocket connection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | Frontend WebSocket connection | accept | Localhost-only MVP, no auth needed per PROJECT.md |
| T-03-02 | Tampering | WebSocket message content | mitigate | Validate commands in backend (BACK-05 from Phase2), frontend sends only valid F/B/L/R/S |
| T-03-03 | Repudiation | Command history | accept | No audit log for MVP, local single-user device |
| T-03-04 | Information Disclosure | WebSocket URL in env | accept | Localhost URL, no sensitive data exposed |
| T-03-05 | Denial of Service | Rapid gamepad input | mitigate | FRONT-07: only send on direction change, deadzone prevents jitter (FRONT-06) |
| T-03-06 | Elevation of Privilege | Frontend code execution | accept | Standard React app, no special privileges, runs in browser sandbox |
</threat_model>

<verification>
- `cd apps/frontend && npx tsc --noEmit` passes with no errors
- `cd apps/frontend && npx vite build` completes successfully
- All CSS files (app.css, control-pad.css, status-bar.css) are deleted
- `.env` file exists with `VITE_WS_URL=ws://localhost:3001`
- StatusBar displays "Backend" instead of "WebSocket"
- useWebSocket hook reads URL from `import.meta.env.VITE_WS_URL`
- No .js files remain in src directory (except possibly setupTests.js)
- Tailwind classes applied: grid layout, pill badges, color tokens from @theme
</verification>

<success_criteria>
1. UI displays connection status with "Backend" label that updates when WebSocket connects/disconnects (FRONT-01)
2. Manual buttons (F, B, L, R, S) send commands via WebSocket when clicked (FRONT-02)
3. Last sent command is displayed on screen with Tailwind styling (FRONT-03)
4. All styling uses Tailwind utilities, no component CSS files remain
5. WebSocket URL configured via VITE_WS_URL env variable, defaults to ws://localhost:3001
6. Frontend builds successfully with `vite build`
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-react-ui-gamepad-control/03-01-SUMMARY.md`
</output>
