---
phase: 03-frontend-react-ui-gamepad-control
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/frontend/src/hooks/use-gamepad.ts
  - apps/frontend/src/app.tsx
  - apps/frontend/src/types.ts
autonomous: true
requirements: [FRONT-04, FRONT-05, FRONT-06, FRONT-07, FRONT-08]
user_setup: []
must_haves:
  truths:
    - "Gamepad stick input maps to correct robot commands with visible feedback (FRONT-04, FRONT-05)"
    - "Analog stick deadzone prevents jitter from triggering commands (FRONT-06)"
    - "Commands only sent on direction change, not continuously (FRONT-07)"
    - "WebSocket auto-reconnects if backend restarts (FRONT-08)"
  artifacts:
    - path: "apps/frontend/src/hooks/use-gamepad.ts"
      provides: "Gamepad polling with deadzone (0.15) and direction detection"
      contains: "getDirectionFromAxes"
    - path: "apps/frontend/src/app.tsx"
      provides: "Direction change detection, sends command only on change"
      contains: "prevDirection"
    - path: "apps/frontend/src/hooks/use-websocket.ts"
      provides: "Auto-reconnect logic (2s interval)"
      contains: "autoReconnect"
  key_links:
    - from: "apps/frontend/src/app.tsx"
      to: "useGamepad direction"
      via: "useEffect watches direction, compares to prevDirection"
      pattern: "direction !== prevDirection.current"
    - from: "apps/frontend/src/app.tsx"
      to: "useWebSocket send"
      via: "sendCommand calls send() with JSON message"
      pattern: "send\\(JSON.stringify"
    - from: "apps/frontend/src/app.tsx"
      to: "autoReconnect"
      via: "useEffect calls autoReconnect when wsConnected changes"
      pattern: "autoReconnect\\(\\)"
---

<objective>
Implement gamepad support with proper deadzone handling, direction-change-only command sending, and ensure WebSocket auto-reconnect works.

Purpose: Complete gamepad functionality (FRONT-04, FRONT-05, FRONT-06, FRONT-07) and verify WebSocket reconnection (FRONT-08). Align message format with backend expectation (Phase 1 D-19: `{ type: "command", data: "F" }` vs current `{ type: "command", command: cmd }`).

Output: Fully functional gamepad control with deadzone, WebSocket communication with auto-reconnect.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/PROJECT.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/ROADMAP.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/REQUIREMENTS.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/phases/03-frontend-react-ui-gamepad-control/03-CONTEXT.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/src/hooks/use-gamepad.ts
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/src/hooks/use-websocket.ts
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/src/app.tsx
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/src/types.ts
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/backend/src/index.ts
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/backend/src/types.ts
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/phases/03-frontend-react-ui-gamepad-control/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align WebSocket message format with backend expectation</name>
  <files>apps/frontend/src/app.tsx, apps/frontend/src/hooks/use-websocket.ts</files>
  <read_first>
    - apps/frontend/src/app.tsx (current: sends `{ type: "command", command: cmd }`)
    - apps/backend/src/index.ts (current: receives message, calls `isValidCommand(message)` on raw string)
    - apps/backend/src/types.ts (isValidCommand expects plain string like "F", not JSON)
    - 03-CONTEXT.md (inconsistency noted: Phase 1 D-19 specified `{ type: "command", data: "F" }`)
  </read_first>
  <action>
    **CRITICAL DECISION (per Phase 1 D-19 carry-forward):** Backend expects PLAIN TEXT commands (F, B, L, R, S) per isValidCommand() in backend/types.ts which validates raw string. Frontend currently sends JSON: `{ type: "command", command: cmd }`.
    
    **Decision**: Align frontend to send PLAIN TEXT commands (matching what backend actually expects per BACK-03).
    
    1. Update app.tsx sendCommand function:
       ```typescript
       const sendCommand = useCallback(
         (cmd: Direction) => {
           send(cmd);  // Send plain text "F", "B", etc. (not JSON)
           setLastCommand(cmd);
         },
         [send],
       );
       ```
    2. Update use-websocket.ts send function to accept string (not JSON):
       - Keep send function as: `const send = useCallback((data: string) => { ... }, []);`
       - It already sends `wsRef.current.send(data)` which is correct for plain text
    
    **Note**: This means we're aligning with what backend ACTUALLY does (isValidCommand on raw message), not what Phase 1 D-19 SAID it should do (JSON with data field). Backend Phase 2 implementation took precedence.
  </action>
  <acceptance_criteria>
    - `apps/frontend/src/app.tsx` sends `send(cmd)` with plain command string (not JSON.stringify)
    - `grep -c "JSON.stringify" apps/frontend/src/app.tsx` returns 0 (no JSON serialization)
    - `grep -n "send(cmd)" apps/frontend/src/app.tsx` returns 1 (sendCommand calls send with cmd)
    - Backend isValidCommand() expects plain "F", "B", etc. (already correct in backend)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | grep -i "error" | wc -l</automated>
  </verify>
  <done>Frontend sends plain text commands (F/B/L/R/S) matching backend expectation, removed JSON serialization</done>
</task>

<task type="auto">
  <name>Task 2: Verify and enhance gamepad hook (deadzone, direction detection)</name>
  <files>apps/frontend/src/hooks/use-gamepad.ts</files>
  <read_first>
    - apps/frontend/src/hooks/use-gamepad.ts (current: implements getDirectionFromAxes, deadzone 0.15)
    - 03-CONTEXT.md (D-06: deadzone 0.15 already implemented, keep this value)
    - apps/frontend/src/types.ts (Direction type: "F" | "B" | "L" | "R" | "S")
  </read_first>
  <action>
    **Verify current implementation is correct** (per CONTEXT: useGamepad already implements deadzone + direction detection):
    
    1. Verify deadzone constant is 0.15:
       - `const DEADZONE = 0.15;` (already correct per D-06)
    2. Verify getDirectionFromAxes function handles all 5 directions:
       - Neutral (both axes < deadzone) → "S"
       - Y dominant, Y negative → "F" (forward)
       - Y dominant, Y positive → "B" (backward)
       - X dominant, X negative → "L" (left)
       - X dominant, X positive → "R" (right)
    3. If gamepad[0] is not the Steam Deck gamepad, add logic to find the correct gamepad:
       ```typescript
       const gp = Array.from(gamepads).find(g => g?.id.includes('Steam')) || gamepads[0];
       ```
       (Per CONTEXT: "may need to find Steam Deck gamepad by ID if multiple detected")
    4. Ensure direction state only updates when direction actually changes (current implementation uses setDirection which will re-render, but app.tsx handles the "only send on change" logic via prevDirection ref)
    5. Delete `apps/frontend/src/hooks/use-gamepad.js` if exists (cleanup .js files)
  </action>
  <acceptance_criteria>
    - `apps/frontend/src/hooks/use-gamepad.ts` has `DEADZONE = 0.15` (per D-06)
    - `apps/frontend/src/hooks/use-gamepad.ts` getDirectionFromAxes returns correct 5 directions (F/B/L/R/S)
    - `apps/frontend/src/hooks/use-gamepad.ts` handles gamepad discovery (finds Steam Deck gamepad if multiple)
    - `grep -c "DEADZONE = 0.15" apps/frontend/src/hooks/use-gamepad.ts` returns 1
    - `grep -c "Steam" apps/frontend/src/hooks/use-gamepad.ts` returns 0 or 1 (optional enhancement)
    - `apps/frontend/src/hooks/use-gamepad.js` does NOT exist
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | grep -i "error" | wc -l</automated>
  </verify>
  <done>useGamepad hook verified: deadzone 0.15, correct direction mapping, optional Steam Deck gamepad discovery added</done>
</task>

<task type="auto">
  <name>Task 3: Verify direction-change-only sending in app.tsx</name>
  <files>apps/frontend/src/app.tsx</files>
  <read_first>
    - apps/frontend/src/app.tsx (current: useEffect with `direction !== prevDirection.current` check)
    - 03-CONTEXT.md (FRONT-07: only sends command on direction change, not continuously)
    - apps/frontend/src/hooks/use-gamepad.ts (returns direction state)
  </read_first>
  <action>
    **Verify FRONT-07 implementation is correct** (only send on direction change):
    
    1. Verify app.tsx has direction change detection:
       ```typescript
       useEffect(() => {
         if (direction !== prevDirection.current) {
           sendCommand(direction);
           prevDirection.current = direction;
         }
       }, [direction, sendCommand]);
       ```
       (Already implemented correctly per current code)
    2. Verify prevDirection is a useRef (not useState) to avoid re-renders:
       - `const prevDirection = useRef<Direction>("S");` (already correct)
    3. Verify autoReconnect is called when connection state changes:
       ```typescript
       useEffect(() => {
         autoReconnect();
       }, [wsConnected]);
       ```
       (Already implemented, triggers reconnection when wsConnected becomes false)
    4. Add Tailwind styling to show current direction visually (user feedback for FRONT-04, FRONT-05):
       ```tsx
       <div className="text-center">
         <span className="text-sm text-gray-400">Current direction: </span>
         <span className="text-accent font-bold">{direction}</span>
       </div>
       ```
       (Per FRONT-04, FRONT-05: "visible feedback" requirement)
  </action>
  <acceptance_criteria>
    - `apps/frontend/src/app.tsx` has direction change check: `direction !== prevDirection.current`
    - `apps/frontend/src/app.tsx` uses `prevDirection = useRef<Direction>("S")` (not useState)
    - `apps/frontend/src/app.tsx` calls `autoReconnect()` in useEffect dependent on wsConnected
    - `apps/frontend/src/app.tsx` displays current direction with Tailwind styling (visible feedback)
    - `grep -c "prevDirection.current = direction" apps/frontend/src/app.tsx` returns 1
    - `grep -c "Current direction" apps/frontend/src/app.tsx` returns 1 (visible feedback)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | grep -i "error" | wc -l</automated>
  </verify>
  <done>Direction-change-only sending verified, visible direction feedback added with Tailwind styling</done>
</task>

<task type="auto">
  <name>Task 4: Verify WebSocket auto-reconnect and add connecting state to UI</name>
  <files>apps/frontend/src/app.tsx, apps/frontend/src/hooks/use-websocket.ts, apps/frontend/src/components/status-bar.tsx</files>
  <read_first>
    - apps/frontend/src/hooks/use-websocket.ts (current: autoReconnect with 2s timeout)
    - apps/frontend/src/app.tsx (calls autoReconnect when wsConnected changes)
    - apps/frontend/src/components/status-bar.tsx (from Plan 03-01: shows Backend status with pill badges)
    - 03-CONTEXT.md (D-11: show "Connecting..." while reconnecting)
  </read_first>
  <action>
    **Verify FRONT-08 (WebSocket auto-reconnect) and implement D-11 (Connecting state):**
    
    1. Verify use-websocket.ts autoReconnect logic:
       - Sets timer for 2000ms when not connected and no existing timer (already implemented)
       - Clears timer on successful connection (already implemented in onopen)
       - Calls connect() after timeout (already implemented)
    2. In app.tsx, pass `connecting` state from useWebSocket to StatusBar:
       ```typescript
       const { connected: wsConnected, connecting, send, autoReconnect } = useWebSocket();
       ```
    3. Update StatusBar component to accept and display `connecting` state:
       - Add `connecting?: boolean` to StatusBarProps interface (done in 03-01 Plan)
       - When `connecting` is true and `wsConnected` is false, show "Connecting..." text
       - Pill badge shows "Backend" with yellow/amber color for connecting state
    4. Implement connecting state display in StatusBar:
       ```tsx
       const isBackEndConnecting = connecting && !wsConnected;
       // In JSX:
       <div className={`px-3 py-1 rounded-full text-sm font-medium ${
         wsConnected ? 'bg-success text-success-text' :
         isBackendConnecting ? 'bg-yellow-600 text-yellow-100' :
         'bg-error text-error-text'
       }`}>
         {wsConnected ? '✓ Backend' : isBackendConnecting ? '⟳ Connecting...' : '✗ Backend'}
       </div>
       ```
       (Per D-11: show "Connecting..." state while WebSocket is reconnecting)
  </action>
  <acceptance_criteria>
    - `apps/frontend/src/hooks/use-websocket.ts` has autoReconnect with 2000ms interval (FRONT-08)
    - `apps/frontend/src/app.tsx` passes `connecting` state to StatusBar
    - `apps/frontend/src/components/status-bar.tsx` shows "⟳ Connecting..." when connecting (D-11)
    - `apps/frontend/src/components/status-bar.tsx` uses Tailwind classes for 3 states: connected (green), connecting (yellow), disconnected (red)
    - `grep -c "Connecting..." apps/frontend/src/components/status-bar.tsx` returns 1
    - `grep -c "isBackendConnecting" apps/frontend/src/components/status-bar.tsx` returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>WebSocket auto-reconnect verified (FRONT-08), "Connecting..." state displayed in StatusBar (D-11) with proper Tailwind styling</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gamepad → Browser | Steam Deck gamepad input crosses into browser via Gamepad API |
| Browser → Frontend | Gamepad axes/direction crosses into React state |
| Frontend → Backend WebSocket | Command strings sent via WebSocket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-------------|-------------|-----------------|
| T-03-07 | Spoofing | Gamepad API | accept | Local Steam Deck only, gamepad ID check optional (low risk) |
| T-03-08 | Tampering | Gamepad input | mitigate | Deadzone (0.15) prevents jitter (FRONT-06), direction validation in app.tsx |
| T-03-09 | Tampering | WebSocket commands | mitigate | Only valid directions sent (F/B/L/R/S), plain text not JSON (simpler, matches backend) |
| T-03-10 | Information Disclosure | Direction state | accept | Direction letter (F/B/L/R/S) displayed in UI, no sensitive data |
| T-03-11 | Denial of Service | Gamepad rapid input | mitigate | FRONT-07: only send on direction change, prevents command spam |
| T-03-12 | Elevation of Privilege | Gamepad API | accept | Runs in browser sandbox, no special privileges |
</threat_model>

<verification>
- `cd apps/frontend && npx tsc --noEmit` passes with no errors
- `cd apps/frontend && npx vite build` completes successfully
- Frontend sends PLAIN TEXT commands (F/B/L/R/S) not JSON (matches backend expectation)
- useGamepad hook has deadzone 0.15, correct 5-direction mapping
- app.tsx only sends command on direction change (prevDirection ref comparison)
- StatusBar shows "Connecting..." state with yellow styling (D-11)
- Auto-reconnect logic verified: 2s interval, clears on success
- Visible direction feedback displayed in UI (Tailwind styled)
- No .js files remain in src/hooks/ directory
</verification>

<success_criteria>
1. Gamepad stick input maps to correct robot commands with visible feedback (FRONT-04, FRONT-05)
2. Analog stick deadzone prevents jitter from triggering commands (FRONT-06, deadzone 0.15)
3. Commands only sent on direction change, not continuously (FRONT-07)
4. WebSocket auto-reconnects if backend restarts (FRONT-08, 2s interval)
5. Frontend sends plain text commands (F/B/L/R/S) matching backend expectation
6. StatusBar shows "Connecting..." state while reconnecting (D-11)
7. Current direction displayed with visible feedback (Tailwind styling)
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-react-ui-gamepad-control/03-02-SUMMARY.md`
</output>
