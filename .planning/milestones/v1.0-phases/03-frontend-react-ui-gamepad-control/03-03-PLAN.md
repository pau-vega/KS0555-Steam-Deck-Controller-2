---
phase: 03-frontend-react-ui-gamepad-control
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - apps/frontend/src/hooks/use-websocket.test.ts
  - apps/frontend/src/hooks/use-gamepad.test.ts
  - apps/frontend/src/components/control-pad.test.tsx
  - apps/frontend/src/components/status-bar.test.tsx
  - apps/frontend/src/app.test.tsx
autonomous: true
requirements: [FRONT-01, FRONT-02, FRONT-03, FRONT-04, FRONT-05, FRONT-06, FRONT-07, FRONT-08]
user_setup: []
must_haves:
  truths:
    - "All hooks (useWebSocket, useGamepad) have unit tests with mocks (D-12, D-13, D-14)"
    - "All components (ControlPad, StatusBar) have tests verifying render + interaction (D-15)"
    - "Test suite covers key scenarios, ~10-15 tests total (D-16)"
  artifacts:
    - path: "apps/frontend/src/hooks/use-websocket.test.ts"
      provides: "Unit tests for useWebSocket hook with WebSocket mock"
      contains: "vi.fn"
    - path: "apps/frontend/src/hooks/use-gamepad.test.ts"
      provides: "Unit tests for useGamepad hook with navigator.getGamepads mock"
      contains: "getGamepads"
    - path: "apps/frontend/src/components/control-pad.test.tsx"
      provides: "Component tests for ControlPad with button click verification"
      contains: "fireEvent.click"
    - path: "apps/frontend/src/components/status-bar.test.tsx"
      provides: "Component tests for StatusBar with status display verification"
      contains: "toBeInTheDocument"
    - path: "apps/frontend/src/app.test.tsx"
      provides: "Updated App component tests"
      contains: "Robot Controller"
  key_links:
    - from: "use-websocket.test.ts"
      to: "vi.fn() WebSocket mock"
      via: "Mock global WebSocket constructor"
      pattern: "WebSocket.*=.*vi.fn"
    - from: "use-gamepad.test.ts"
      to: "navigator.getGamepads mock"
      via: "Mock navigator.getGamepads for gamepad tests"
      pattern: "getGamepads.*=.*vi.fn"
    - from: "control-pad.test.tsx"
      to: "onCommand callback"
      via: "Test button clicks fire onCommand with correct command"
      pattern: "onCommand.*expect.*toHaveBeenCalled"
---

<objective>
Implement test suite for Phase 3: unit tests for hooks (useWebSocket, useGamepad) and component tests for UI (ControlPad, StatusBar, App).

Purpose: Fulfill test strategy decisions (D-12 through D-16): ~10-15 tests total, mock WebSocket with `vi.fn`, mock `navigator.getGamepads`, verify render + interaction for all components.

Output: Complete test suite with Vitest + @testing-library/react, all tests passing.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/PROJECT.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/ROADMAP.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/REQUIREMENTS.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/phases/03-frontend-react-ui-gamepad-control/03-CONTEXT.md
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/src/hooks/use-websocket.ts
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/src/hooks/use-gamepad.ts
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/src/components/control-pad.tsx
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/src/components/status-bar.tsx
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/src/app.tsx
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/vite.config.ts
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/apps/frontend/package.json
@/Users/pauvelascogarrofe/Documents/KS0555-Steam-Deck-Controller/.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useWebSocket hook unit tests</name>
  <files>apps/frontend/src/hooks/use-websocket.test.ts</files>
  <read_first>
    - apps/frontend/src/hooks/use-websocket.ts (current: reads VITE_WS_URL from env, has connecting state)
    - 03-CONTEXT.md (D-12, D-13: unit test hook, mock WebSocket with vi.fn)
    - apps/frontend/vite.config.ts (test configuration)
    - apps/frontend/package.json (vitest, @testing-library/react deps)
  </read_first>
  <action>
    Create `apps/frontend/src/hooks/use-websocket.test.ts` with Vitest tests:
    
    1. Mock global WebSocket:
       ```typescript
       const mockWs = {
         onopen: null,
         onclose: null,
         onerror: null,
         send: vi.fn(),
         close: vi.fn(),
         readyState: WebSocket.OPEN,
       };
       global.WebSocket = vi.fn(() => mockWs) as any;
       ```
    
    2. Test cases (6 tests):
       - "connects on mount" — verifies WebSocket constructor called with VITE_WS_URL
       - "sets connected=true on open" — triggers onopen, verifies connected state
       - "sets connected=false on close" — triggers onclose, verifies connected state
       - "sets connecting state during connection" — verify connecting is true initially
       - "send sends data when connected" — calls send(), verifies mockWs.send called
       - "send does nothing when disconnected" — readyState not OPEN, send() does nothing
       - "autoReconnect calls connect after timeout" — vi.useFakeTimers, advance 2000ms
    
    3. Use @testing-library/react renderHook for testing hooks:
       ```typescript
       import { renderHook, waitFor } from '@testing-library/react';
       import { useWebSocket } from './use-websocket';
       ```
    
    4. Cleanup: vi.clearAllMocks(), vi.useRealTimers() in afterEach
  </action>
  <acceptance_criteria>
    - `apps/frontend/src/hooks/use-websocket.test.ts` exists with 6+ test cases
    - `grep -c "vi.fn" apps/frontend/src/hooks/use-websocket.test.ts` returns at least 3 (WebSocket mock, send mock)
    - `grep -c "renderHook" apps/frontend/src/hooks/use-websocket.test.ts` returns at least 1
    - `grep -c "WebSocket" apps/frontend/src/hooks/use-websocket.test.ts` returns at least 3 (mock + assertions)
    - Tests cover: connect, open, close, connecting state, send (connected), send (disconnected), autoReconnect
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx vitest run src/hooks/use-websocket.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>useWebSocket hook has 6+ unit tests with mocked WebSocket, all tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Create useGamepad hook unit tests</name>
  <files>apps/frontend/src/hooks/use-gamepad.test.ts</files>
  <read_first>
    - apps/frontend/src/hooks/use-gamepad.ts (current: getDirectionFromAxes, deadzone 0.15, rAF polling)
    - 03-CONTEXT.md (D-12, D-14: unit test hook, mock navigator.getGamepads)
    - apps/frontend/src/types.ts (Direction type)
  </read_first>
  <action>
    Create `apps/frontend/src/hooks/use-gamepad.test.ts` with Vitest tests:
    
    1. Mock navigator.getGamepads:
       ```typescript
       const mockGamepad = {
         axes: [0, 0],  // Default: neutral (S)
         id: 'Steam Deck Gamepad',
       };
       navigator.getGamepads = vi.fn(() => [mockGamepad as any]);
       ```
    
    2. Mock requestAnimationFrame:
       ```typescript
       vi.spyOn(window, 'requestAnimationFrame').mockImplementation((cb: any) => {
         setTimeout(() => cb(0), 0);
         return 0;
       });
       ```
    
    3. Test cases (6 tests):
       - "returns direction S when axes are neutral (within deadzone)" — axes [0, 0], expect "S"
       - "returns direction F when Y axis negative (forward)" — axes [0, -0.5], expect "F"
       - "returns direction B when Y axis positive (backward)" — axes [0, 0.5], expect "B"
       - "returns direction L when X axis negative (left)" — axes [-0.5, 0], expect "L"
       - "returns direction R when X axis positive (right)" — axes [0.5, 0], expect "R"
       - "sets gamepadConnected=true when gamepad present" — valid gamepad, expect true
       - "sets gamepadConnected=false when no gamepad" — getGamepads returns [null], expect false
       - "deadzone: does not change direction for small movements" — axes [0.1, 0.1], expect "S" (within 0.15)
    
    4. Use renderHook from @testing-library/react
    5. Mock getDirectionFromAxes directly or test through hook (simpler: test hook)
    
    **Note**: Since getDirectionFromAxes is not exported, test through the hook's direction state.
  </action>
  <acceptance_criteria>
    - `apps/frontend/src/hooks/use-gamepad.test.ts` exists with 7+ test cases
    - `grep -c "getGamepads" apps/frontend/src/hooks/use-gamepad.test.ts` returns at least 2 (mock + assertions)
    - `grep -c "direction" apps/frontend/src/hooks/use-gamepad.test.ts` returns at least 5 (all direction tests)
    - `grep -c "deadzone\|0.15" apps/frontend/src/hooks/use-gamepad.test.ts` returns at least 1
    - Tests cover: all 5 directions (F/B/L/R/S), gamepad connected/disconnected, deadzone
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx vitest run src/hooks/use-gamepad.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>useGamepad hook has 7+ unit tests with mocked getGamepads, all tests passing</done>
</task>

<task type="auto">
  <name>Task 3: Create ControlPad component tests</name>
  <files>apps/frontend/src/components/control-pad.test.tsx</files>
  <read_first>
    - apps/frontend/src/components/control-pad.tsx (current: 5 buttons grid, Tailwind classes from Plan 03-01)
    - 03-CONTEXT.md (D-15: component tests verify render + interaction, button clicks fire onCommand)
    - apps/frontend/src/types.ts (Direction type)
  </read_first>
  <action>
    Create `apps/frontend/src/components/control-pad.test.tsx` with @testing-library/react tests:
    
    1. Setup:
       ```typescript
       import { render, screen, fireEvent } from '@testing-library/react';
       import { ControlPad } from './control-pad';
       ```
    
    2. Mock onCommand callback:
       ```typescript
       const mockOnCommand = vi.fn();
       ```
    
    3. Test cases (5 tests):
       - "renders all 5 control buttons" — queryByLabelText or getByText for ▲, ◀, ■, ▶, ▼
       - "calls onCommand with 'F' when forward button clicked" — fireEvent.click, expect(mockOnCommand).toHaveBeenCalledWith('F')
       - "calls onCommand with 'B' when backward button clicked" — fireEvent.click, expect 'B'
       - "calls onCommand with 'L' when left button clicked" — fireEvent.click, expect 'L'
       - "calls onCommand with 'R' when right button clicked" — fireEvent.click, expect 'R'
       - "calls onCommand with 'S' when stop button clicked" — fireEvent.click, expect 'S'
       - "buttons are disabled when disabled prop is true" — render with disabled={true}, verify button disabled attribute
       - "Stop button has distinct styling" — verify btn-s class or specific styling (optional)
    
    4. Use aria labels or button text to query elements (▲, ◀, ■, ▶, ▼)
  </action>
  <acceptance_criteria>
    - `apps/frontend/src/components/control-pad.test.tsx` exists with 7+ test cases
    - `grep -c "fireEvent.click" apps/frontend/src/components/control-pad.test.tsx` returns at least 5 (one per button)
    - `grep -c "toHaveBeenCalledWith" apps/frontend/src/components/control-pad.test.tsx` returns at least 5 (assertions for each command)
    - `grep -c "disabled" apps/frontend/src/components/control-pad.test.tsx` returns at least 1 (disabled state test)
    - Tests cover: render all buttons, click each button (F/B/L/R/S), disabled state
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx vitest run src/components/control-pad.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>ControlPad component has 7+ tests verifying render and button click interactions, all tests passing</done>
</task>

<task type="auto">
  <name>Task 4: Create StatusBar component tests</name>
  <files>apps/frontend/src/components/status-bar.test.tsx</files>
  <read_first>
    - apps/frontend/src/components/status-bar.tsx (current: shows "Backend" + "Gamepad" with Tailwind pill badges from Plan 03-01)
    - 03-CONTEXT.md (D-08: label as "Backend", D-15: component tests verify render + status labels)
  </read_first>
  <action>
    Create `apps/frontend/src/components/status-bar.test.tsx` with @testing-library/react tests:
    
    1. Setup:
       ```typescript
       import { render, screen } from '@testing-library/react';
       import { StatusBar } from './status-bar';
       ```
    
    2. Test cases (4 tests):
       - "shows 'Backend' label (not 'WebSocket')" — render with wsConnected={true}, verify "Backend" text present (D-08)
       - "shows checkmark when Backend connected" — render with wsConnected={true}, verify "✓" present
       - "shows X when Backend disconnected" — render with wsConnected={false}, verify "✗" present
       - "shows 'Connecting...' when connecting" — render with connecting={true}, wsConnected={false}, verify "Connecting..." text
       - "shows Gamepad connected state" — render with gamepadConnected={true}, verify "✓ Gamepad" present
       - "shows Gamepad disconnected state" — render with gamepadConnected={false}, verify "✗ Gamepad" present
       - "applies success color when Backend connected" — verify bg-success class or green styling
       - "applies error color when Backend disconnected" — verify bg-error class or red styling
    
    3. **Note**: Need to add `connecting?: boolean` prop to StatusBar (done in Plan 03-01 or 03-02)
    
    4. Query by text content: `getByText('Backend')`, `getByText('Gamepad')`, `getByText('Connecting...')`
  </action>
  <acceptance_criteria>
    - `apps/frontend/src/components/status-bar.test.tsx` exists with 6+ test cases
    - `grep -c "Backend" apps/frontend/src/components/status-bar.test.tsx` returns at least 2 (render + assertion)
    - `grep -c "Gamepad" apps/frontend/src/components/status-bar.test.tsx` returns at least 2 (connected + disconnected)
    - `grep -c "Connecting" apps/frontend/src/components/status-bar.test.tsx` returns at least 1
    - `grep -c "✓\|✗" apps/frontend/src/components/status-bar.test.tsx` returns at least 2 (checkmark and X)
    - Tests cover: Backend label (not WebSocket), connected/disconnected states, Gamepad state, Connecting state
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx vitest run src/components/status-bar.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>StatusBar component has 6+ tests verifying status display, "Backend" label, and connecting state, all tests passing</done>
</task>

<task type="auto">
  <name>Task 5: Update App.test.tsx with comprehensive tests</name>
  <files>apps/frontend/src/app.test.tsx</files>
  <read_first>
    - apps/frontend/src/app.test.tsx (current: only renders heading)
    - apps/frontend/src/app.tsx (current: uses useWebSocket, useGamepad, ControlPad, StatusBar)
    - 03-CONTEXT.md (D-15: component tests verify render + interaction)
  </read_first>
  <action>
    Update `apps/frontend/src/app.test.tsx` with more comprehensive tests:
    
    1. Mock hooks:
       ```typescript
       vi.mock('./hooks/use-websocket', () => ({
         useWebSocket: () => ({ connected: true, connecting: false, send: vi.fn(), autoReconnect: vi.fn() }),
       }));
       vi.mock('./hooks/use-gamepad', () => ({
         useGamepad: () => ({ direction: 'S' as const, gamepadConnected: true }),
       }));
       ```
    
    2. Keep existing test: "renders robot controller heading"
    
    3. Add tests (4 tests):
       - "displays StatusBar component" — verify "Backend" text present
       - "displays ControlPad component" — verify button ▲, ◀, ■, ▶, ▼ present
       - "displays last command" — verify "Last command:" text present (initially "S")
       - "displays current direction" — verify direction display (added in Plan 03-02)
       - "displays last command update after send" — integrate with mock send to verify update
    
    4. Render App component:
       ```typescript
       render(<App />);
       ```
    
    5. Clean up mocks in afterEach:
       ```typescript
       afterEach(() => { vi.clearAllMocks(); });
       ```
  </action>
  <acceptance_criteria>
    - `apps/frontend/src/app.test.tsx` has 5+ test cases (up from 1)
    - `grep -c "vi.mock" apps/frontend/src/app.test.tsx` returns at least 2 (mock useWebSocket, mock useGamepad)
    - `grep -c "Backend\|ControlPad\|Last command\|direction" apps/frontend/src/app.test.tsx` returns at least 4 (status, buttons, last command, direction)
    - `grep -c "Robot Controller" apps/frontend/src/app.test.tsx` returns at least 1 (keep existing test)
    - Tests cover: heading, StatusBar presence, ControlPad presence, last command display, direction display
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx vitest run src/app.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>App.test.tsx updated with 5+ comprehensive tests mocking hooks, all tests passing</done>
</task>

<task type="auto">
  <name>Task 6: Run full test suite and verify ~10-15 tests pass</name>
  <files>apps/frontend/src/hooks/use-websocket.test.ts, apps/frontend/src/hooks/use-gamepad.test.ts, apps/frontend/src/components/control-pad.test.tsx, apps/frontend/src/components/status-bar.test.tsx, apps/frontend/src/app.test.tsx</files>
  <read_first>
    - All test files created in Tasks 1-5
    - apps/frontend/package.json (test script: "vitest run")
    - 03-CONTEXT.md (D-16: ~10-15 tests total, no coverage gate enforced)
  </read_first>
  <action>
    1. Run full test suite:
       ```bash
       cd apps/frontend && npx vitest run
       ```
    
    2. Verify test count: expect 10-15 tests total (D-16):
       - use-websocket.test.ts: 6 tests
       - use-gamepad.test.ts: 7 tests
       - control-pad.test.tsx: 7 tests
       - status-bar.test.tsx: 6 tests
       - app.test.tsx: 5 tests
       - Total: ~31 tests (exceeds D-16 minimum, which is fine)
    
    3. If any tests fail:
       - Debug and fix failing tests
       - Common issues: missing mocks, incorrect query selectors, async timing
    
    4. Verify no test file covers the same thing twice (no duplicates)
    
    5. **Optional**: Delete `apps/frontend/src/App.test.js` if exists (cleanup .js files)
    
    6. Final verification: `npx vitest run` exits with code 0, all tests pass
  </action>
  <acceptance_criteria>
    - `cd apps/frontend && npx vitest run` passes with ALL tests passing (exit code 0)
    - Test output shows 10+ tests passing (likely ~31 based on above count)
    - `grep -r "test\\|it\\(" apps/frontend/src/**/*.test.{ts,tsx} | wc -l` returns 10+ (test case count)
    - No tests are duplicated across files
    - `apps/frontend/src/App.test.js` does NOT exist (cleanup)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/frontend && npx vitest run 2>&1 | grep -E "PASS|FAIL|Tests|test" | tail -15</automated>
  </verify>
  <done>Full test suite passes with 10+ tests (expect ~31), all hooks and components tested per D-12 through D-16</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Code → Source | Test files import and test source modules |
| Test Runner → Browser API | jsdom environment mocks browser APIs (WebSocket, navigator) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-------------|-------------|-----------------|
| T-03-13 | Tampering | Test mocks | accept | Mocks only in test files, don't affect production code |
| T-03-14 | Information Disclosure | Test code | accept | Tests don't handle sensitive data, all mocks are fake |
| T-03-15 | Elevation of Privilege | Vitest runner | accept | Standard test framework, runs in local dev environment |
</threat_model>

<verification>
- `cd apps/frontend && npx vitest run` passes with ALL tests (10+ tests, expect ~31)
- `npx tsc --noEmit` passes with no errors in frontend
- `npx vite build` completes successfully
- All hooks (useWebSocket, useGamepad) have unit tests with proper mocks
- All components (ControlPad, StatusBar, App) have rendering and interaction tests
- Test count meets D-16 expectation (~10-15 minimum, we have ~31)
- No .js test files remain (cleanup done)
</verification>

<success_criteria>
1. useWebSocket hook has 6+ unit tests with mocked WebSocket (D-12, D-13)
2. useGamepad hook has 7+ unit tests with mocked getGamepads (D-12, D-14)
3. ControlPad component has 7+ tests verifying render + button interactions (D-15)
4. StatusBar component has 6+ tests verifying status display + "Backend" label (D-08, D-15)
5. App component has 5+ comprehensive tests with mocked hooks (D-15)
6. Full test suite passes: `npx vitest run` shows all tests passing (10+ tests)
7. Frontend builds successfully with `vite build`
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-react-ui-gamepad-control/03-03-SUMMARY.md`
</output>
