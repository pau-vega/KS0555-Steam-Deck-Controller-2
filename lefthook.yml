pre-commit:
  parallel: true
  commands:
    format:
      run: pnpm format
      stage_fixed: true
    lint:
      run: pnpm lint
    typecheck:
      run: pnpm typecheck

commit-msg:
  commands:
    commitlint:
      run: npx --no -- commitlint --edit "$LEFTHOOK_COMMIT_MSG"
